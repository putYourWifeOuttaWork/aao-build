<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>AAO_Bound_Key__c</fullName>
    <caseSensitive>false</caseSensitive>
    <description>Written by: the cardinality guard. Opportunity id plus bounded path. Unique, so a path that keeps hitting its bound raises one flag that updates rather than one per attempt.</description>
    <externalId>true</externalId>
    <label>Bound Key</label>
    <length>120</length>
    <required>false</required>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>true</unique>
</CustomField>
