# AAO Board

> **The version lives on the stamp line below and nowhere else. Read this document first in every session and update it last.**

**v2.9 · 9 August 2026, session close · THE HALVING IS MET AND RATIFIED; THE LANE RULING AND THE STABILITY PROBE LAND; THE EXECUTION SEED BELOW IS THE NEXT SESSION'S LANE. THE RESULT: run `pf0808-h1`, ~57 s in-org COLD (66 s shell), 6 model calls, worst callout 27.0 s, bytes 41/41, designator quotes 4/4, trap 0/0, ledger 41/41/28 HELD, commit `5b8b3d1`, verified by design against the org (map values, notes, and the Buyer Role citation fix all live). The day's arc: 59 callouts to 6, ~150 s of identification to ~4 s of resolution, 105 s to 57. BOTH HALVING MOVES RATIFIED: verify parallelized as CONJUNCT-HOMOGENEITY buckets (52.5 s to 20.8 s; the split is permanent whenever sentiment rides with anything else; `AAO_Pass.verify(runKey, bucket, shardCount, shardIndex)` shards further; the 50-stretch is one driven shard away and rides the accuracy phase, no standalone run); and call 0 concurrent with the reads. THE CACHE REORDER IS LIVE AND MEASURED: in_tok per read 23,804 to 3,220; the sequential probe showed cache_read 27,436, ~88% of a read's input at ~10% price; parallel reads cannot share within a run, which is mechanical, not a failure. THE LANE RULING (design under delegation, veto open): tokens versus latency is DISPATCHER CONFIG, one code path, one flag, never a design fork. THE BATCH LANE (the nightly window, throughput-bound): SERIAL, call 0 first exactly as the dispatch law says, reads reusing the cached artifact prefix, token-optimal, wall irrelevant at 21-36 concurrent, and THE COST MODEL PRICES THIS LANE. THE EXPRESS LANE (seller-facing freshness): h1's parallel graph, ≤60 met; the dispatch law bends lawfully and namedly: reads launch speculatively and the scope verdict gates PAIR ADMISSION, with NONE or ACCOUNT dropping speculative output unpersisted and the tokens as the lane's stated premium; the admission-drop semantics BUILD with the lane flag before any non-fixture artifact runs express. The call-0 parallelization was a structural move made inside an execution stretch; it is ruled and kept via the lanes, and that class of move parks first from here. THE STABILITY FINDING, the accuracy phase's new subject: tg1 and h1 are the SAME SHAPE disagreeing with itself on identical bytes (44 pairs then 41; Adam at Political Structure plus Evaluator, then Inner Circle plus a buyer-role collision; Dan stable Supporter on both against p1's Non-Supporter). THE STABILITY PROBE: three identical runs, same shape, same frozen fixture, purge between, diffed mechanically on the merge key; STABLE versus FLICKERING per establishment; the rate reported; nothing tuned against its first output; Matthew still grades ONE comparison, tg1 against Pass 1. NEW OPEN TO MATTHEW, methodology, from h1's honest refusal (Adam: Evaluator AND User both established, the vendor field holds one, the writer held and noted): WHAT WINS WHEN TWO BUYER ROLES BOTH VERIFY: recency on the evidence-occurred clock, an authored precedence ladder, or hold-plus-flag; hold-and-note stands until he rules. THE FOLD DISCIPLINE FOR THE STRETCH AHEAD: this Board plus inbox stamps 27 through 32 are the authority; the DEEP FOLDS (Charters §P8 supersede marks and the lane text, Architecture, Model & Flow entities for the pair ledger, span set, receipt, and create-leg record, Glossary terms) are DEFERRED DELIBERATELY to the next strong session, batched with the span-set build so the charter text moves once, not twice; A LESSER SESSION NEVER REWRITES THIS BOARD OR ANY LAW DOCUMENT: it appends to the inbox and parks. Companions: inbox stamps 30 through 32; `review/pf0808-h1/timings.md`; the machine read sheet regenerated at v1.1 (the suspect list turned over: five answered, five new).**

**v2.8 · 8 August 2026, session close · THE GATE RAN AND THE HALVING ORDER GOVERNS; FOUR RULING SETS BANKED (inbox stamps 28 through 31); THE EXECUTION WORKING MODE BEGINS. THE GATE: run `pf0808-tg1`, frozen Project Farma through the rebuilt pipeline, ~105 seconds source through projection COLD, 6 model calls plus one honest call-0 retry, worst callout 31.8 s, no split fired. THE 59-CALLOUT SHAPE IS DEAD: deterministic resolution took 234 ms plus one 5.5 s typed-request call where the old call 2 spent ~150 s across 59 transactions; 71 callouts became 6; the whole rebuilt pipeline costs less than the old call 2 did alone. Design verified the run against the org before any verdict: the three map rows true to the report, named criteria live ("Competitive pricing within budget" projected; "Salesforce-native, methodology-driven" held Partial), both placeholder CR rows retired lawfully in place, Dan Lewis re-created and CAUGHT by the create-leg record (MK-00000001), the purge's first use clean under the human-watermark guard. MATTHEW'S VERDICT: good news, band miss accepted (past acceptable 60-90, under the 120 fail line), and the number must HALVE: target ≤60 s, stretch 50. THE HALVING ORDER (thirtieth stamp): parallelize the two verify batches (~79 s expected; confirm whether the two-batch split is family-homogeneity, which would make the shape permanent); build the CACHE PREFIX REORDER now (artifact-first prompts so every call after the first reuses the artifact prefix, cutting seconds AND tokens, production-cold included); the call 3 downsize comparison rides the accuracy phase, the largest remaining lever on both seconds and dollars. THE RULINGS BANKED SINCE v2.7, each carried in its inbox stamp: THE ADMISSION FILTER (28: deterministic, metadata-only, zero model, zero fetch, in front of intake itself; sales and account management only, keyed on licensed roles of organizer or internal participants plus the external-participant and bounded-committee gates; the cost chosen and written: sales evidence on non-sales calls is lost by design; builds after the gate and before any automated funnel); CALL 0 SLIMS (28: the mentioned-person emission cut as vestigial; ONE resolver service with two invocation moments, call 0 up front and the resolution stage on demand; two implementations would be a bug); THE PURGE-AND-RERUN PROTOCOL (28: delete only what is enumerable as ours, run-keyed rows and create-leg-recorded contacts; seeds stay; human-watermarked rows never; the create-leg record is what makes purge lawful); THE SPAN SET (29: a pair's evidence becomes one to three byte-located verbatim spans stitched across adjacent turns, elicitation marked, entering as CITED EVIDENCE never invisible context; call 3 stays blind on exactly the cited set; builds at the accuracy phase; deq1/deq2 the acceptance specimens); THE RECEIPT CONFIRMED as one thin object with two jobs, the did-we-read record and the per-call telemetry home, under the new OBJECT-BUDGET LAW (29: every new object justifies itself against the enterprise install budget); THE 90-SECOND SPLIT TRIGGER (29: any stage crossing 90 s of callout wall completes its REMAINING work in a continuation, never redoes, never retries bigger; split units are artifact partition, request batch, claim batch); THE THROUGHPUT REQUIREMENT (29: 10,000 transcripts in the 8-hour window is ~21 concurrent pipelines at 60 s, ~11 at 30; the deciding ceilings, org async concurrency and gateway rate limits, are unverified and measured after the halving rerun); THE LOOKS-RIGHT STANDARD (30: records generate properly, traps hold, timings in band, nothing graded until the accuracy phase); THE CHARTERS SHARE THE PASS (31: Problems, Politics, and Process add QUESTIONS to the same two reads and CLAIMS to verify, never new reads; dependencies live at the join as code order, person rows then insight cards then edges, an edge with a missing endpoint HELD never guessed, then process and assessment off our own ledger; the four-charter future is never a multiple of the People pass, always an increment; the cost model prices it before Problems declares); SINGLE PROJECTION (31, design direction, proposal-first: OUR LEDGER IS AUTHORITATIVE and the vendor surface is its projection, so downstream computation reads our answers and vendor DML batches once at the end of the run); SCALE COST IS TOKENS, NOT SECONDS (30: parallelism buys latency only; the reorder and the downsize buy both). THE STANDING QUESTION THE GATE OPENED, deliberately not adjudicated: the two shapes DISAGREED on identical bytes (Dan Lewis Non-Supporter at Pass 1, Supporter at tg1; Adam Decision Maker then Evaluator plus Political Structure; 44 pairs located where the sweep found 59, 14 corroborated); at most one map is right and neither is graded; MATTHEW GRADES tg1 AGAINST PASS 1 at the accuracy rerun. Two small defects from design's org verification: the Option C note's Buyer Role lines carry no date-and-speaker citation (fix rides the halving work); CR-00000000's vendor projection on Emerson still displays the word CRITERION, marked and reported, disposition Matthew's by hand. THE EXECUTION WORKING MODE (31), because the strong-model budget is finite and the design is banked: stamps 27 through 31 plus this Board are LAW for the stretch ahead; execution sessions on any model RELAY, BUILD, RUN, and VERIFY per the verification sheet and the looks-right standard and never redesign; a structural question that surfaces is PARKED in the inbox's "For the next design session" list with options and costs; strong sessions become short and rare, rulings only. Companions: inbox stamps 28 through 31; the machine read sheet v1.0, a new satellite carrying the design as gradeable assertions, Matthew's audit instrument, regenerated on shape changes. Charters, Architecture, Model & Flow, and the Glossary owe the v2.7 and v2.8 rulings at their next bumps, held behind the halving work.**

**v2.7 · 8 August 2026 · MATTHEW'S EFFICIENCY DIRECTIVE RESHAPES THE PASS, and the fifty-nine is diagnosed. PROJECT FARMA PASS 1 REACHED THE LIVE MAP: three rows (Adam Pfeiffer Supporter and Decision Maker, Dan Lewis Non-Supporter and Evaluator created through the old participant leg with no email, Kayla Stanley coverage only), the demo-narration trap HELD (no fictional person or fact reached the map), 59 of 59 quotes byte-exact, the criterion correctly held off the map by the whole-only gate WITH the collapse defect confirmed live (one CR row, interpretation "CRITERION", reachable from no person). THE COST: 59 callouts on a five-person transcript, diagnosed with certainty from CODE's own report: call 2 was handed 59 pairs, returned one disposition per call regardless of batch size, and fell back to batch=1, one callout per pair. THE DIAGNOSIS UNDERNEATH THE DEFECT, Matthew's, ratified: call 2 was doing a DETERMINISTIC job by inference; who spoke a pair is a byte lookup (offset to turn to call 0's resolved roster), never a judgment. THE DIRECTIVE: stop everything; fix criteria first; then test TIME ONLY, normalized through projection on the frozen Project Farma fixture, target one to two minutes; freeze all other charter and architecture changes until the gate reports. THREE RULINGS, Matthew's, 8 August evening, all in the twenty-seventh inbox stamp. (1) THE FAMILY SWEEP IS SUPERSEDED: CALL 1 IS TWO COMPREHENSIVE READS of the whole artifact, each carrying ALL declared families' question sets in one prompt, pairs exactly as now (verbatim, meaning, designator with quoted introducing span, no identity attached); two reads, not one and not four, because inference drops things and the reads' independent misses are the recall hedge; his words: "let us do 2 comprehensive reads, identify answers to all 6 questions possible on each read (not 4) so as to let the inference differences help each other." The 5 August sweep and its two-to-eleven measurement stand as history, one fixture under the tuned-behaviour law; the merge key for the two reads is contract plus byte-range INTERSECTION plus voiced meaning, NEVER string equality on quotes ("similar meaning answer pair twice. not byte for byte though"); union survives, corroboration marks and never gates. (2) CALL 2 IS REBUILT AS THE RESOLUTION STAGE and identification is DELETED as a model job: speaker attach is deterministic in Apex; designators run the existing ladder deterministically; the model leg fires only on the remainder (off-artifact names, title-only references, uncontacted speakers, unresolvable antecedents) and emits TYPED RESOLUTION REQUESTS, never query text or clauses, with Apex composing and executing every query (his intent "it infers which queries are required and what the clauses are" carried as typed fields, design's correction recorded overrulable: model-authored clause text is an injection surface); a second bounded call only to separate returned candidates; creation on ladder-zero behind the create-leg record, WHICH NOW SHIPS WITH THE REBUILD, Dan Lewis back-filled as its first row. RESOLUTION ATTACHES SUBJECTS, NEVER SPEAKERS: a note's author stays its only speaker and the speaker-requirement gates keep pricing; notes and emails are where this stage is the MAIN path and the request schema is channel-agnostic by design. (3) THE CRITERIA FIX IS FIRST: the criterion's name is a descriptive word or words STATING THE CONDITION THAT MUST BE TRUE FOR THEM TO MAKE THE POSITIVE DECISION, derived from the establishing words; key opportunity plus name; the collapsed CR history corrects through the lawful path; subject linkage wired so criterion claims are reachable from the person's evidence list. THE TIMING GATE follows the rebuild: one run, frozen Project Farma, TIME AND CALLS ONLY, org-query and DML time reported separately from model time (the rebuild moves cost there), no accuracy claims riding; the ACCURACY RERUN (two reads against the graded sets) is named now and comes after the gate, before the shape is called proven. Clean-transcript arithmetic: call 0, two reads, zero resolution calls, one or two verify batches; four or five model calls, then projection, which is local math. Companion: CODE inbox twenty-seventh stamp. Charters §P8 owes the supersede marks at its next bump, held by the freeze itself until the gate reports; this stamp is the authority in the interim. [v2.8: the gate ran and reported; see the head stamp.]**

**v2.6 · 8 August 2026 · TWO THINGS: THE REFUSAL DIAGNOSIS IS DONE AND IT CORRECTS THIS BOARD'S OWN v2.5 STAMP, AND TITLES AND PRONOUNS ARE RULED. THE CORRECTION FIRST, the companion-wins law running in its proper direction: design read all eighteen of a23's stored refusal reasons directly from the sandbox (`AAO_Verification_Note__c`, per row, the D1 lesson paying a second time) and the new satellite `aao-a23-refusal-diagnosis-v1_0.md` carries them verbatim. THE FINDING: CALL 3 IS NOT BROKEN, THE CONTRACTS ARE. Every note is coherent and faithful to a contract's own text; the tuning is real and lives in the CONTRACT PROPOSITIONS, not the verifier. v2.5's sentence "Call 3's restraint was never a principle; it was a tuning artifact" is half right and marked below: the restraint WAS a tuning artifact, and it was the contracts' tuning, not call 3's. Thirteen of eighteen are TWO single clauses: six on Political Structure written as an AUTHORITY test with no companion proposition for membership-without-authority (an ABSENT proposition, additive to repair), five on the sentiment wrong-scope guard, which is correct code exposing a METHODOLOGY TENSION that only Matthew can settle: MENTOR is conjunctive and org-directed, and on real speech those pull apart (Jefferson pushing his own legal to expedite our paperwork advances us materially and voices nothing about us). THE STANDING CAVEAT: a23 is one late-stage contracting call, so an unknown share of 0-of-18 is STAGE rather than generalisation, and only Matthew's production transcripts at MIXED STAGES can separate them; they move from wanted to LOAD-BEARING. CODE's instruction narrows to a report: ABSENT versus PRESENT-AND-TOO-NARROW, per row, against the declared contract texts; call 3's text does not move. THEN THE RULING: Matthew ruled titles and pronouns, option B with the mechanism, closing the one structural decision v2.5 left open. Pronouns are anaphoric, his argument: a person is introduced before ever being "he," so CALL 1 RESOLVES THE REFERENT IN CONTEXT and emits a DESIGNATOR (name or title) with the INTRODUCING SPAN QUOTED AND BYTE-LOCATED, making a wrong referent visible where the byte check is blind. The resolver service gains a TITLE RUNG (Contact.Title scoped to the account, plus map-row contacts; deterministic synonym list as seed metadata). One wording correction of design's, recorded overrulable: Matthew said "call 3 do attribution on paper"; call 3 stays blind by law, and the pass attributes at call 1 and the join, which he confirmed. "A few reasonable tries" is the ladder's rungs, once, in order, journalled, never a retry loop. THE FREE GUARD: A TITLE CAN LINK BUT NEVER CREATE, because a Contact requires a LastName and "CFO" has none, so the fuzziest designators are structurally barred from the creation leg. Resolved designators pass the internal check like anyone else; buq2/Pat stays the acceptance case. Cost: one ladder rung, one call 1 emission rule (version moves per the two-halves law), one held terminal that already exists; no new model calls. All seven a23 dropped pairs now have a ruled mechanism and become the build's regression specimens. Companion stamps: the refusal diagnosis v1.0 (new satellite), CODE inbox twenty-fourth stamp. Still waiting on Matthew: MENTOR, poq5/poq6, buq2 versus the gate, the Board convention.**

**v2.5 · 8 August 2026 · a23 IS GRADED. THE FIRST HUMAN GRADING ON SPEECH NOTHING WAS TUNED AGAINST, AND IT INVERTS A STANDING ASSUMPTION OF THIS PROJECT. THE RESULT: Matthew graded 32 of 35 located pairs TRUE, so the READ is finding real things at 91% on foreign speech; then upheld precision 70% (7 of 10), REFUSALS 0 OF 18 CORRECT against 21 of 23 on the training set, 7 of 7 dropped pairs true, and across all 35 pairs EXACTLY ONE correct establishment reaches full strength on the map. THE READ IS EXCELLENT AND THE PIPELINE DESTROYS IT. Everything lost was lost by call 3 (18) and by the roster-built candidate list (7). NEW LAW, and it is the session's central finding: A BEHAVIOUR MEASURED ONLY ON THE FIXTURE IT WAS TUNED AGAINST CAN BE A PROPERTY OF THE TUNING RATHER THAN OF THE DESIGN, AND NOTHING INSIDE THAT FIXTURE CAN TELL THE TWO APART. ~~Call 3's restraint was never a principle; it was a tuning artifact.~~ **[v2.6: half wrong, marked in place, never deleted. The restraint was a tuning artifact, but the tuning lives in the CONTRACT PROPOSITIONS, which call 3 applied faithfully on all eighteen. The diagnosis satellite is the record; the law stands with its subject corrected.]** The adjudication sheet's "recall, not restraint, is the open problem" is marked wrong in place at its v1.0. MATTHEW RULED TWICE MORE: RESOLUTION IS RE-ENTRANT, a charter that meets a person it cannot resolve calls the resolver rather than dropping the pair, built as the resolution half of call 0 becoming a CALLABLE SERVICE while the scope half stays once per Source, because a family read that could re-open scope could flip the pass's grain from inside; and SHADOW INVERTS TO LAST RESORT, ladder first, link the existing Contact, create only on ladder-zero, shadow only where a Contact cannot exist, never merely because resolution failed before the call. HIS GENERALISING ARGUMENT CHANGES THE SCHEDULE, NOT JUST THE DESIGN: emails and notes will always name people no roster carries, so on every artifact type after transcripts the mentioned-person path is not the tail case, it is the only path. DESIGN'S OWN RECORDED EXPECTATION IS SHOWN WRONG BY DESIGN, before CODE spent a run producing the number: of the 7 dropped pairs, ONE resolves by name to an existing Contact, one is a name that is OURS (Pat, buq2, the next Wendy arriving through the predicted door on a real row), two are a title with no name, three are bare pronouns. TWO GRADES GO BACK TO MATTHEW rather than folding as law, and one structural decision is open and must not be built to: titles and pronouns. **[v2.6: ruled, option B with the mechanism; see the stamp above.]** Companion stamps: adjudication sheet v1.0 (§11 carries the full grading), CODE inbox twenty-second stamp. Charters, Architecture, Model & Flow, Glossary still owe a23's three rulings plus these two at their next bumps.**

**v2.4 · 8 August 2026 · A CORRECTION OF THIS DOCUMENT'S OWN STATE, AND a23 FOLDED. THE CORRECTION FIRST, because it is the kind this project exists to make: v2.3's closing sentence, "The 29 July run had NOT landed when this session opened and stays first in CODE's queue," IS WRONG and is marked wrong in place in the stamp below. IT LANDED. It ran as `a23` and STANDS VERIFIED: provenance 35 of 35 on unseen speech, the ledger honest, None working, and the regression N/A defect caught by the verification sheet on its first live outing and fixed (sheet corrected to v1.1 by its own user, the loop working in both directions). THE MECHANISM OF THE ERROR, named so it is designed against rather than rediscovered: two design sessions ran concurrently; one absorbed a23 and re-stamped the inbox twice while the other executed the foreign-corpus research task and wrote the inbox back as a FULL-FILE REPLACE FROM A READ TAKEN AN HOUR EARLIER. The stale write was itself overwritten minutes later, so nothing of CODE's was lost, and that is ordering luck rather than discipline. THE STANDING HAZARD IS NOW NAMED: A FULL-FILE REPLACE FROM A STALE READ DESTROYS A CONCURRENT FOLD AND REPORTS SUCCESS, the silent-success hazard reaching the document plane. The inbox survived because its convention is ADDITIVE, a new stamp on top of preserved ones; this Board is the document at risk, because its own convention is rewrite-whole, which is the maximum-damage write pattern under concurrency. Design's rule from here, effective now: RE-READ IMMEDIATELY BEFORE EVERY WRITE, AND ADD RATHER THAN RECONSTRUCT. Whether the Board's rewrite-whole law itself survives concurrency is a STRUCTURAL DECISION LEFT OPEN FOR MATTHEW, with options and costs in section 4. WHAT a23 CHANGED, folded: three rulings of Matthew's (THE INTERNAL-DOMAIN GATE APPLIES TO CLAIM SUBJECTS AT THE JOIN, so a seller can never be a buying-committee member by construction rather than by verifier judgment; A CRITERION'S IDENTITY IS ITS NAME, keyed on opportunity plus name; and the stamp repair RATIFIED as writer-supplied, never caller-supplied), plus the cost journal accepted as the standing model with the cache prefix reorder approved and QUEUED BEHIND MATTHEW'S GRADING so the comparison stays valid. AND PEOPLE GOT BIGGER, Matthew's nineteenth-stamp ruling: THE MENTIONED-PERSON/SHADOW PATH BUILDS NOW, AS PART OF PEOPLE, BEFORE IT CLOSES, on his first principle and its number, 7 of 35 located pairs on unseen speech dropping silently including Fatema Choudray, the signer of the deal; the machine-created-Contact disposition is owed PROPOSAL-FIRST because it is org data rather than our ledger. THE LIVE BLOCKER ON THE FIRST UNSEEN NUMBER IS NOW ONE THING: MATTHEW'S INVERTED GRADING OF a23. Design cannot prepare his grading sheet until a23's row export reaches the project; it has not. **[v2.5: closed. The export landed, the sheet was built twice (the first was unusable and he said so), and he graded it. The grading is §11 of the adjudication sheet at v1.0.]** THE FOREIGN CORPUS SURVIVES and is restored to CODE at the inbox's twentieth stamp, strictly behind the four items above. Companion stamps: verification sheet v1.1, CODE inbox twentieth stamp, corpus record v1.0. Charters, Architecture, Model & Flow, Glossary still owe a23's three rulings at their next bumps.**

**v2.3 · 8 August 2026 · THE UNSEEN CORPUS SESSION EXECUTED THE v2.2 SEED AND THE CORPUS GROWS BY TWO. THE SWEEP VERDICT, about twenty-eight probes, four public transcripts inventoried, recorded in full in the new satellite `aao-unseen-corpus-record-v1_0.md`: full, real, named-speaker, deal-stage B2B sales conversations effectively do not exist on the open web; pre-award vendor presentations retreat into closed sessions and untranscribed subcommittees; council vendor demos exist only as unlabeled captions, and labeling them ourselves is inferred attribution the research already gates; the scarcity itself is evidence for the training-set hazard. MATTHEW RULED IN SESSION, three times. (1) BOTH FIXTURES LAND. (2) THE NEGATIVE-CONTROL LAW, new counting law: the one verified real, uncut, speaker-labeled full conversation found (The Pitch S10 E12, a real founder pitching five real investors, 59 minutes, published deliberately) runs as a NEGATIVE CONTROL: real speech that is not a deal, so the correct output is little to nothing, honestly refused or routed NEITHER; anything established on it is an over-read on foreign speech and COUNTS TOWARD PRECISION, FULLY; recall it cannot measure, and the freeze list says so. The first precision measurement on speech from outside the org's culture, which no authored transcript can provide. Nothing is seeded for it. (3) THE AUTHORED FIXTURE IS THE MECHANISM TEST, Matthew's scenario: OpenText selling Aviator to Wells Fargo's US investment bank; deal-shaped so every family, the criteria path, and call 0's deal side exercise on foreign format (WebVTT, a raw form the intake has never ingested); AUTHORED, MECHANISM ONLY, never counting toward precision or recall, because grading our own authorship is the training-set problem. Both delivered as files with hashes (`85ccada3…` real; `be8e5e95…` authored), seed specs, specimen map, and freeze-entry drafts in the record; CODE normalizes, freezes NF1-only with composed stamps, seeds per spec, and runs them BEHIND the standing queue. NEITHER SUBSTITUTES FOR THE PRODUCTION PULL: the only honest positive foreign measurement remains Matthew's three-plus production transcripts, owed, the gap open and named. THE DELEGATION PRECEDENT, Matthew's: corpus composition under the standing counting laws is design's call under the delegated mode from here, both positions recorded per decision; this session's decision surface is the precedent instance. ALSO CLEANED AT THIS REWRITE: gate 2 is CLOSED by the seventeenth stamp's trailing-90-day ruling, and the stale three-line-grade text the v2.2 body still carried is superseded in place here. Companion stamps: the corpus record v1.0 (new), the CODE inbox eighteenth stamp. ~~The 29 July run had NOT landed when this session opened and stays first in CODE's queue.~~ **[v2.4: WRONG, marked in place, never deleted. It had landed as a23 and was verified while this stamp was being written; the session that wrote this sentence was working from a stale read and did not check before asserting state. The companion-wins law applies and the Board is corrected: the inbox's stamped body was current and this Board was not. Also wrong by consequence: this stamp's pointer to "the CODE inbox eighteenth stamp" as its companion, since that stamp was overwritten; the foreign-corpus content now lives at the twentieth stamp.]****

**v2.2 · 8 August 2026 · THREE RULINGS AT THE CLOSE, Matthew's. (1) COVERAGE IS A TRAILING-90-DAY CONVERSATION COUNT, simple arithmetic, no ontology: how many conversations with this person in the last three months; a first call brings a person to one; the count maps to the vendor values as built. GATE 2 CLOSES on this ruling, because the current derivation already matches it exactly (Neeraja 1, Ryan 2, Jefferson 2, all occasions inside the window). Consequence, proposal-first for CODE: a windowed count must RE-DERIVE as calls age out, not only when new evidence arrives. (2) THE HOT/COLD SPLIT IS RULED DESIGN DIRECTION: the claims chain (claims, claim bases, the volume-heavy history) streams to Data 360 after roughly 30 days; Salesforce holds the hot window; D360 is the memory plane as the architecture already carries. Two consequences recorded so they are designed rather than discovered: the replay law (counters, verdicts, retirement) must work across the split, reading D360 past the hot window; and the 90-day coverage window is the archive's first consumer, since it looks back further than the hot store. Proposal-first, never ordinary work; the D360 platform facts and the one unverified ConnectApi item apply. (3) THE FOREIGN FIXTURE: the next session carries a research task to obtain an enterprise-grade 30-to-45-minute B2B sales-call transcript. PREFER FOUND REAL SPEECH from the web; an authored-realistic transcript is the fallback and tests intake and format only, NEVER counting toward precision or recall; Matthew may override in session. Design writes the sandbox account, contacts, and opportunity for whichever fixture lands. Matthew's production transcripts remain wanted in addition, not instead. [v2.3: executed; the corpus grew by two under the rulings above.]**

**What this is.** The one document a session opens first: current state, the complete law list, the open items with owners, and the next steps. When this document and a companion disagree, the companion's stamped body wins on substance and this board is corrected; the board wins only on what is open versus closed. Prior board versions do not exist; everything a reader needs is in this one.

---

## 1 · The documents, the actors, the locations

| Document | Authoritative for |
|---|---|
| **Primer** (`aao-primer.md`) | A stranger's first read; orientation and reading order |
| **Board** (`aao-board.md`) | State, the complete locked ledger, sequence, seeds |
| **Glossary** (`aao-glossary.md`, v2.7) | Vocabulary, abbreviations, the named laws |
| **Architecture** (`aao-architecture.md`, v4.12) | Every ruling with its reasoning; the loop era is the v4.2 through v4.12 heads |
| **Model & Flow** (`aao-model-and-flow.md`, v1.5) | Entities, keys, fields as built |
| **Charters** (`aao-charters.md`, v3.5) | The AI: every model call, the contracts, the resolver, the recovered ontologies |
| **Adjudication sheet** (`aao-adjudication-sheet.md`, v1.0) | Every human grading; §11 is a23, the first on unseen speech; §10 is the map read |
| **Refusal diagnosis** (`aao-a23-refusal-diagnosis-v1_0.md`) | a23's eighteen refusal reasons verbatim from the org, grouped by mechanism; the stage-versus-generalisation caveat |
| **Verification sheet** (`aao-verification-sheet.md`, v1.1) | Design's per-run checks, the written procedure |
| **Map read sheet** (`aao-map-read-sheet-v0_3.md`) | Matthew's read procedure on the product surface |
| **Machine read sheet** (`aao-machine-read-sheet-v1_0.md`) | The design as bare gradeable assertions, tagged code versus model; Matthew's audit instrument; regenerated on shape changes |
| **Corpus record** (`aao-unseen-corpus-record-v1_0.md`) | The foreign fixtures: sweep verdict, provenance, hashes, counting statuses, seed specs, freeze-entry drafts |

**The actors.** MATTHEW rules methodology and grades output, inverted. DESIGN (the session holding this document set) argues structural decisions before writing, defaults technical calls with both positions recorded, folds rulings, verifies every export per the verification sheet, re-stamps the inbox last; **corpus composition under the standing counting laws is design's call**, the delegation precedent recorded in the corpus record. CODE is the builder: a Claude Code agent operating a git repository with the sandbox connection and NO production connection; a fresh CODE session's entire seed is "read the inbox top stamp." **[v2.8: THE EXECUTION WORKING MODE begins, thirty-first stamp: execution sessions on any model relay, build, run, and verify per the sheets and NEVER redesign; structural questions PARK in the inbox's "For the next design session" list; design's strong sessions are short and rare, rulings only.]**

**MORE THAN ONE DESIGN SESSION CAN BE LIVE AT ONCE, and on 8 August two were.** The document set has no version token on write, so a full-file replace from a stale read silently destroys a concurrent fold. **Standing procedure until ruled otherwise: re-read the exact path immediately before writing it, and write additively where the document's convention allows.**

**The locations.** These documents live in the claude.ai project (design's side) and as `docs/` in CODE's repository, carried between them by Matthew or the project. CODE's repository also holds: `BUILD_JOURNAL` (the only numeric record), `review/<run-id>/` (every run's export), `review/gate3/frozen-fixtures.md` (the freeze list, which disambiguates what stamps cannot), and the regression set. Exports reach design as files through Matthew. **Design also now holds a direct read connection to the sandbox, which closes the export hand-off gap that bit a21, a23 and the grading-sheet build; the refusal diagnosis was produced through it in one pass.** The CODE inbox (`CODE-INBOX.md`) is the standing channel; its top stamp is CODE's current instruction. The two foreign fixture raws and the capture sidecar live in the project beside the corpus record.

## 2 · Current state · 8 August 2026, session close

**Org:** sandbox `altify--aossb2` (`00DWD00000DV7iT2AT`, IsSandbox true, re-verified at the gate's verification). Production (`altify.my.salesforce.com`, org `00DHn000006jYatMAE`) read-only unconditionally; `altify-pbo` never read. **A connection's identity is unverified until the runtime names the org: query Organization before doing any work through any MCP connection, every session.**

**THE REBUILT PIPELINE IS BUILT, GATED, AND VERIFIED**, artifact to live vendor map: call 0 slimmed (one model read: two one-way scope questions with quotes plus the which-opportunity pick; deterministic ladder in Apex; NONE does not dispatch), call 1 as TWO COMPREHENSIVE READS in parallel (all declared families per read, byte-range merge, corroboration marks never gates), the RESOLUTION STAGE (speaker attach as byte lookup in Apex, 234 ms and zero model calls on PF; typed-request model leg only for the remainder; creation on ladder-zero behind the live create-leg record), blind verify in keyed family batches, the join through the single writer, projection with per-dimension watermarks, coverage computed. Commit `64a2605`; suite 420 ran, the only failure the standing org-resident `ConvertToOpportunityTest`. **The gate: `pf0808-tg1`, ~105 s COLD, 6 model calls, worst callout 31.8 s, ledger 44/44/29 HELD, demo-narration trap HELD, criteria named and lawful. Verified by design against the org.** The old 59-callout shape is dead: 71 callouts to 6, ~150 s of call 2 to 5.7 s of resolution. **THE HALVING RERUN `pf0808-h1` then MET the target: ~57 s in-org COLD, verify parallelized as conjunct buckets (52.5 to 20.8), call 0 hidden under the reads, the cache reorder live (in_tok 23,804 to 3,220 per read; sequential cache_read 27,436), the Buyer Role note citation fixed on the live map. Commit `5b8b3d1`. Verified by design against the org.**

**THE ACTIVE WORK IS THE ACCURACY PHASE (thirty-second stamp): the span set builds (twenty-ninth stamp item 1), the call 3 downsize comparison runs, the two-read shape runs against s1 and a23 with full exports, THE STABILITY PROBE runs (three identical runs on frozen PF, purge between, mechanical diff on the merge key, shardCount=2 driven inside), and Matthew's grading sheet for tg1 against Pass 1 is prepared with criterion names and internal marking derived.** The three runs so far disagree on identical bytes (Adam: Decision Maker at p1, Political Structure plus Evaluator at tg1, Inner Circle plus a held buyer-role collision at h1; Dan: Non-Supporter at p1, Supporter at tg1 and h1; pairs 59, 44, 41). **At most one map is right and none is graded; the probe measures the flicker mechanically and Matthew's one grading settles the truth.** Then the admission filter proposal, the single-projection proposal, the receipt object, and the concurrency measurement. The a23 record and its diagnosis stand unchanged (the adjudication sheet §11, the refusal diagnosis satellite, the stage caveat riding everything); the absent-versus-too-narrow report and the additive propositions wait behind the accuracy phase.

**The precision gate on the product surface passed at the 7 August map read and that grading stands.** What a23 adds is that the map read was performed on values derived from the training set. **A gate passed on the training set is a calibration, not a result** (section 3). The timing gate adds the complement: **a looks-right map at speed is a mechanics result, never a truth result; the truth question is the accuracy phase's.**

## 3 · Locked · the complete ledger, not open to relitigating

- **LAW #1.** No ALTF package-version dependency, ever. Feature detection, never version checks. The system runs with Altify absent; the ontology ships as our seed metadata, org-overridable.
- **No metadata, triggers, or logic on any ALTF or native object.** Opportunity and Account never written by the product runtime; Contact toggleable (the toggle defaults ON, Matthew's, design's OFF recorded; overrides are picklists because a checkbox cannot express untouched); shadow persons permanent for toggle-off customers and never on the vendor map (`ALTF__Contact__c` is `nillable=false`, verified from the runtime).
- **Production read-only unconditionally. Evidence over inference. A capability claim is unverified until tried from the calling runtime, including claims about our own code.** **A state claim is unverified until the document carrying it is opened by exact path, immediately before it is asserted.** **And a connection's identity is unverified until the runtime names the org.**
- **THE TUNED-BEHAVIOUR LAW (a23, 8 August, and it outranks every number this project produced before it): A BEHAVIOUR MEASURED ONLY ON THE FIXTURE IT WAS TUNED AGAINST CAN BE A PROPERTY OF THE TUNING RATHER THAN OF THE DESIGN, AND NOTHING INSIDE THAT FIXTURE CAN TELL THE TWO APART.** Its evidence: the same charter, contracts and verifier moved from 91% correct refusals to 0% by changing the artifact. **The diagnosis places the tuning: in the CONTRACT PROPOSITIONS, which the verifier applied faithfully.** **A gate passed on the training set is a calibration, not a result.** Every repair verified only on `ec8e7170` re-opens on the first unseen fixture that exercises it; buq6/EVALUATOR is the first confirmed instance.
- **THE STAGE CAVEAT RIDES EVERY SINGLE-FIXTURE FINDING:** one call at one deal stage cannot separate a generalisation failure from a register mismatch; repairs proposed against one fixture's refusals carry that unmeasured risk, stated wherever the finding travels. Mixed-stage production transcripts are the separating instrument.
- **THE GRADING INSTRUMENT IS AN OUTPUT OF THE SYSTEM AND INHERITS ITS BLIND SPOTS (a23).** A gap in what the sheet can show becomes a gap in what the human can grade, and the human's grade is the only ground truth this project has. Two proven instances: withholding the criterion's name made criteria rows ungradeable, and failing to mark an internal person invited a grade putting our own signatory on the customer's committee. **Every sheet from here carries the criterion's name and marks the internal side, both derived, never by hand.** **An empty UNDER channel is only evidence when the grader says it is**, otherwise it is not-exercised.
- **THE PASS IS PAIRS; CALL 1 IS A FAMILY SWEEP** (once per declared family, whole artifact, one transaction each; the call count never a function of participant count; questions once per transcript; cost scales with transcript length, never roster size). Call 2 identifies only, from the closed candidate list, NONE and AMBIGUOUS honest. Call 3 verifies the proposition blind and nothing else; the placement never reaches the reader; the courtesy conjunct is part of the sentiment proposition (stance toward us or our solution, never the meeting or the occasion). **[v2.7: the first clause and the call 2 clause are SUPERSEDED by Matthew's 8 August rulings below (two comprehensive reads; the resolution stage). What survives of this entry unchanged: pairs as the atomic output, questions never per person, cost never a function of roster size, call 3's blindness and the courtesy conjunct whole.]**
- **CALL 1 IS TWO COMPREHENSIVE READS (Matthew, 8 August, superseding the family sweep):** each read covers the whole normalized artifact carrying ALL declared families' question sets in one prompt; pairs, designators, and the no-identity law unchanged; two reads because inference misses are independent and the union is the recall hedge. **THE MERGE KEY: contract plus byte-range INTERSECTION plus voiced meaning, never string equality on quotes.** A pair only one read found stands; corroboration is a marker, never a gate; same bytes and question with different meanings both ride to call 3 and the mixed-evidence law prices the survivors; coverage merges conservative (partial unless both reads say full). The sweep's two-to-eleven measurement stands as history, one fixture under the tuned-behaviour law. **[v2.8: BUILT AND GATED at pf0808-tg1; the reads ran in parallel, 31.0 plus 31.8 in 33.3 wall.]**
- **IDENTIFICATION IS NOT A MODEL JOB. CALL 2 IS THE RESOLUTION STAGE (Matthew, 8 August):** the speaker of a pair is a byte lookup (offset to containing turn to call 0's resolved roster) in Apex; designators resolve through the deterministic ladder including the title rung; **the model leg fires only on the remainder and emits TYPED RESOLUTION REQUESTS (kind, words as written, introducing quote, side, closed-list candidate or NONE), NEVER query text or clauses; Apex composes and executes every query.** A second bounded call only where returned candidates need separating; NONE and AMBIGUOUS stay honest; AMBIGUOUS is the Identification flag, never a pick. **Resolution attaches SUBJECTS, never speakers**: a note's author is its only speaker forever and the speaker-requirement gates keep pricing; on notes and emails this stage is the MAIN path and the request schema is channel-agnostic. **The create-leg record ships WITH the rebuild, Dan Lewis back-filled as its first row.** **[v2.8: BUILT AND GATED; 234 ms deterministic, one 5.5 s model leg on PF; Dan's re-creation caught live as MK-00000001.]**
- **TIME IS MEASURED BEFORE ACCURACY IS CLAIMED (Matthew, 8 August):** the timing gate runs frozen Project Farma normalized through projection and reports wall clock, call count, and org-query/DML time separately from model time; no accuracy claims ride it; the accuracy rerun against the graded sets follows the gate and precedes any "proven." **[v2.8: the gate RAN, ~105 s COLD, 6 calls, PASSED the fail line; the HALVING ORDER now governs, target ≤60 stretch 50; the looks-right standard (records generate properly, traps hold, timings in band, nothing graded) is the interim standard until the accuracy phase.]**
- **THE ADMISSION FILTER (Matthew, 8 August, twenty-eighth stamp):** deterministic, metadata-only, zero model, zero fetch, in front of intake itself; a transcript is admitted only when call metadata says sales or account management (licensed role on the organizer or an internal participant, an external participant present, committee bounded); a refused call stores nothing and a cheap tally records the refusal; related-record stamps corroborate exclusion only, never decide alone. **The cost chosen and written: sales evidence spoken on non-sales calls is lost by design.** Builds after the halving work, before any automated funnel or pilot ingest.
- **THE OBJECT-BUDGET LAW (Matthew, 8 August, twenty-ninth stamp):** every new object justifies itself against the enterprise install budget; object count and daily row volume are adoption costs; a design that cannot say why a row exists does not get the row. The RUN RECEIPT passes it: one thin object, one row per Source per pass, the did-we-read-it record and the per-call telemetry home, retiring to Data 360 with the claims chain.
- **THE SPAN SET (Matthew, 8 August, twenty-ninth stamp):** a pair's evidence is an ordered set of one to three verbatim spans, each byte-located, stitched across adjacent turns where one idea spans them, another speaker's eliciting words included and marked; **added words enter as CITED EVIDENCE, never invisible context, so the receipt never lies to a future auditor; call 3 stays blind on exactly the cited set.** Coextension survives at the set level; caps proposed before build; deq1/deq2 the acceptance specimens. Builds at the accuracy phase.
- **THE 90-SECOND SPLIT TRIGGER (Matthew, 8 August, twenty-ninth stamp):** any stage crossing 90 seconds of callout wall in one transaction completes its REMAINING work in a continuation call, never redoes, never retries by growing; split units are artifact partition (call 1), request batch (resolution), claim batch (call 3); every split journals.
- **THE CHARTERS SHARE THE PASS (design under delegation, 8 August, thirty-first stamp):** Problems, Politics, and Process add QUESTIONS to the same two comprehensive reads and CLAIMS to verify, never new reads; dependencies live at the join as code order (person rows, then insight cards, then edges where both endpoints exist with a missing-endpoint edge HELD, then process and assessment); **the four-charter future is never a multiple of the People pass, always an increment**, and the cost model prices it before Problems declares. **SINGLE PROJECTION is ruled design direction, proposal-first: our ledger is authoritative, downstream computation reads OUR answers, vendor-surface writes batch once at the end of the run.** **SCALE COST IS TOKENS, NOT SECONDS:** batch parallelism buys latency only; the cache reorder and the call 3 downsize buy both.
- **THE LANE RULING (design under delegation, 9 August, thirty-second stamp):** tokens versus latency is DISPATCHER CONFIG, one code path, one flag. **BATCH LANE** (nightly window): serial, call 0 first per the dispatch law, reads reusing the cached artifact prefix (~88% of read input at ~10% price, measured), token-optimal; the cost model prices this lane. **EXPRESS LANE** (seller-facing): the parallel graph, ≤60 s met; reads launch speculatively and **the scope verdict gates PAIR ADMISSION instead of read launch**, NONE or ACCOUNT dropping speculative output unpersisted with the tokens as the lane's stated premium; the admission-drop semantics build with the lane flag before any non-fixture artifact runs express. **VERIFY BUCKETS ARE CONJUNCT-HOMOGENEOUS AND PARALLEL, permanent**; a bucket that would cross the 90-second trigger drives as concurrent shards.
- **THE STABILITY PROBE (thirty-second stamp):** the two-read shape's run-to-run flicker is measured, never assumed: three identical runs, purge between, mechanical diff on the merge key, STABLE versus FLICKERING per establishment, the rate reported beside the accuracy results; nothing is tuned against its first output; a single run's map is one draw until the probe says otherwise.
- **THE PURGE-AND-RERUN PROTOCOL (Matthew, 8 August, twenty-eighth stamp):** test cycles purge and re-run on frozen fixtures freely; purge deletes ONLY what is enumerable as ours (run-keyed rows, create-leg-recorded contacts); seeds stay; human-watermarked rows never; runs label COLD or WARM and a warm rerun is never reported as cold performance.
- **CALL 0, ALWAYS, SLIMMED (twenty-eighth stamp):** the resolver's read on every Source before the reads; deterministic ladder in Apex first, outputs as evidence; the mentioned-person emission is cut as vestigial (designators own it, demand-driven); call 0 routes and resolves and never establishes. **THE SCOPE READ IS TWO ONE-WAY QUESTIONS**, each yes carrying a byte-locating quote; DUAL is both yes; NEITHER is sayable; a missing side throws; a yes without a quote throws (proven live at the gate's cold attempt). **SIDE-YES WITH DEAL-NONE DOES NOT DISPATCH.** **A PAIR'S SCOPE IS ITS READ'S DECLARED GRAIN.** The stretch-stamp law decides which deal, never which grain. Membership never routes.
- **RESOLUTION IS RE-ENTRANT; SCOPE IS NOT (Matthew, 8 August).** A charter that meets a load-bearing person it cannot resolve calls the resolver rather than dropping the pair. Built as **the resolution half of call 0 becoming a callable service**, same ladder, same evidence outputs, same NONE and AMBIGUOUS honesty, invoked on demand with a designator. **The scope half stays once per Source**, because it is the routing decision and a family read that could re-open it could flip the pass's grain from inside. **[v2.7: this service is the resolution stage's substrate; the ruling stands unchanged. ONE SERVICE, TWO INVOCATION MOMENTS; two implementations would be a bug.]**
- **THE DESIGNATOR RULING (Matthew, 8 August, closing titles and pronouns):** call 1, the only call holding the whole artifact, **resolves a pronoun to its in-artifact introduction and emits the pair with a DESIGNATOR, a name or a title, plus the introducing span quoted and byte-located**, so a wrong referent is visible where the byte check is structurally blind (a wrong name is not an invented quote). The resolver service carries a **TITLE RUNG**: Contact.Title matched against a deterministic synonym list (seed metadata, org-overridable), scoped to the account, plus map-row contacts for the account or opportunity. One match links; more than one is AMBIGUOUS, the Identification flag, never pick; zero is held with its evidence, never dropped, never guessed. **"Reasonable tries" are the ladder's rungs, once, in order, journalled, never a retry loop.** **A TITLE CAN LINK BUT NEVER CREATE**: a Contact requires a LastName, so the fuzziest designators cannot reach the creation leg; only a real name can, behind ladder-zero and the internal check. Call 3 stays blind; attribution lives at call 1 and the join. Resolved designators pass the internal check like anyone else.
- **SHADOW IS LAST RESORT (Matthew, 8 August).** Ladder first; link the existing Contact where one exists; create only on ladder-zero; **shadow only where a Contact cannot exist, never merely because resolution failed before the call.** AMBIGUOUS is not shadow either: it means a Contact exists and we cannot tell which, so it routes to the Identification flag, pair held, nothing linked, nothing created. **The generalising argument, Matthew's, and it changes the schedule: emails and notes always name people no roster carries, so on every artifact type after transcripts the mentioned-person path is the only path, not the tail case.** Its consolation: an email address is a key and a transcribed name is not, so the artifact type that makes this urgent is the one that makes it safe. **Match aggressively, create conservatively** binds hardest on the transcript case.
- **THE MENTIONED PERSON DOES NOT DROP SILENTLY (Matthew, 8 August):** a pair whose person is mentioned in the artifact but absent from every roster ~~resolves through the shadow stage~~ **[v2.5: superseded in place, marked not deleted. It resolves through the re-entrant resolver; shadow is last resort per the law above.]** rather than vanishing. It builds as part of People, before People closes. The number that earned it: 7 of 35 located pairs on unseen speech, including the signer of the deal, **all seven graded TRUE, all seven now carrying a ruled mechanism, all seven the build's regression specimens.** **[v2.8: the path ran live at the gate: Rich's designator went through the model leg, minted a subject, and his pairs were refused at verify, correctly nothing on the map.]**
- **THE OCCURRED-TIME CANDIDATE WINDOW:** candidates are open plus recently closed as of the evidence-occurred clock, never the run date. (Unverifiable in this sandbox; verifies at pilot.)
- **TRUE-OR-NOTHING:** every question points one way; no FALSE, no inversion; negative states establish from their own affirmative words; two-sided questions split into one-way halves.
- **NO ABSTENTION ROWS, at any grain, ever.** The run receipt (confirmed at the twenty-ninth stamp, builds with or immediately after the rebuild) is the did-we-read-it record; one-for-one-for-one arithmetic between stage row sets; `assertOneForOne` returns HELD / INCOMPLETE / BROKEN. **DECLINED-TO-RUN AND RAN-AND-FOUND-NOTHING ARE DIFFERENT FACTS; neither ships the other's export.**
- **THE SENTIMENT COUNTER is Support's mechanism:** one move per distinct verified establishment toward the voiced state, never past it; net per call; clamp ±3; voiced-state ceiling (supporter-grade tops +2, mentor-grade reaches +3); terminals proof-gated, never time-gated; the standing value on the Support Answer row, rebuildable from claims in evidence-occurred order. **MENTOR is conjunctive and org-directed; ENGAGEMENT IS NOT STANCE; COURTESY IS NOT STANCE.** **[OPEN TENSION, Matthew's to settle, surfaced by the diagnosis: conjunctive and org-directed pull apart on real speech; whether acting on our behalf inside their org establishes mentorship without a voiced stance is his ruling and nothing near the wrong-scope guard moves until he makes it.]** **[Twenty-fifth stamp: ruled under his delegation, veto open. MENTOR splits into MENTOR-VOICED (unchanged) plus two additive one-way siblings, MENTOR-ACTS and MENTOR-GUIDES; the guard does not move; DUTY IS NOT DEVOTION is the new guard beside them; the ceiling generalizes to "the strongest state the words establish"; the jump rule stays voiced-only. Build waits behind the halving work and the accuracy phase.]**
- **CLAIMS KEY THE PARTICIPANT.** **CLAIMS RETIRE, NEVER DELETE**; replays exclude retired claims; retraction clears what they carried and stamps its watermark like any write. **PARTIAL ACCRUES, PLACES NOBODY, AND DISPLACES NOTHING**; answer verdicts are monotone over evidence strength; zero live claims is UNVERIFIED, distinct from never-asked. **A TRAP THAT FIRES IS REFUSED AT THE CLAIM.**
- **THE INTERNAL-DOMAIN GATE APPLIES TO CLAIM SUBJECTS AT THE JOIN (Matthew, a23):** a People-family claim whose subject resolves to an internal domain refuses structurally with its reason. **Sellers cannot be buying-committee members by construction, never by verifier judgment.** Existing wrong rows correct through the lawful path, retire and retract. **Its named blind spot is now a live specimen rather than a hypothetical: a mentioned person has no email, so the gate cannot see one, and a23's buq2 names our own signatory for the customer's map. A NAME-BASED INTERNAL CHECK IS A PRECONDITION OF THE MENTIONED-PERSON PATH.**
- **A CRITERION'S IDENTITY IS ITS NAME (Matthew, a23):** the criteria families' pairs carry the criterion's name derived from the establishing words; the key is opportunity plus name; the vendor Subject displays the name. Charter and schema both. **DECISION CRITERIA JOIN PEOPLE; `AAO_Criterion__c` IS OURS** (the fifth subject type; the vendor row is its projection; contracts join by byte-range overlap); DC-F types by provenance; criteria are sparse by nature; buying-process mechanics and approval coaching are never criteria; **partial criteria project nothing until elements complete.** **[v2.7: Matthew's naming refinement: the name is a descriptive word or words STATING THE CONDITION THAT MUST BE TRUE FOR THEM TO MAKE THE POSITIVE DECISION.]** **[v2.8: BUILT, LIVE, AND VERIFIED: "Competitive pricing within budget" named and projected; the partial held unprojected; both placeholder rows retired lawfully with full reasons.]**
- **COVERAGE IS COMPUTED, NEVER EXTRACTED**, from distinct conversations (occasion identity: system ref plus occurred time; **TWO KEYS FOR TWO JOBS**); **coverage is a trailing-90-day conversation count**, re-deriving as calls age out, mechanism proposal-first; `In-depth` deliberately underivable; zero occasions writes nothing. **THE ALIAS, NEVER THE MERGE**; unresolved-unresolved stays visibly split, never-pick.
- **THE HOT/COLD SPLIT IS RULED DESIGN DIRECTION:** the claims chain streams to Data 360 after roughly 30 days; Salesforce holds the hot window; the replay law must work across the split; the 90-day coverage window is the archive's first consumer; proposal-first.
- **THE NORMALIZER STAMP COMPOSES** (`NF1+raw:`), and **it is writer-supplied, never caller-supplied** (Matthew, a23, the watermark law's own logic; the caller's sObject is never rewritten under it). Pre-composition rows keep uncomposed stamps permanently, because **provenance a later process can rewrite is not provenance**. **NF1 is the fixture form; fixtures come from the freeze list, never the org.** **THE TRAINING SET IS NAMED so it can never be counted as unseen; authored fixtures never substitute for customer speech.**
- **THE NEGATIVE-CONTROL LAW (Matthew, 8 August):** real found speech that is not a deal runs as a negative control; the correct output is little to nothing, honestly refused or routed NEITHER; **anything established on it is an over-read on foreign speech and counts toward PRECISION, fully; recall it cannot measure, and the freeze list says so.** Authored fixtures are mechanism tests only and count toward nothing. **Every freeze-list entry states its counting status.** Corpus composition under these counting laws is design's call, both positions recorded per decision.
- **A VERSION STRING COVERS EVERY INPUT THAT CAN CHANGE THE OUTPUT IT STAMPS.** **A WATERMARK IS WRITTEN BY THE WRITER OR IT DOES NOT EXIST**; the one lawful repair class is a named repair whose value is known, which refuses itself where preconditions fail. **A non-blank value our watermark does not claim was put there by a human**, and its converse: a value our watermark does claim is ours and is retractable. **`touched` means our own recorded projection watermark, never the native `ALTF__*_Last_Modified__c` stamp**, which is non-null for precisely the rows a human edited.
- **A MACHINE-CREATED ROW WE CANNOT ENUMERATE IS A ROW WE CAN NEVER DISOWN.** The create-leg record ships before the creation leg; machine-created org data is marked and reported, never deleted by us. **[v2.7: it ships WITH the resolution-stage rebuild, Dan Lewis back-filled.]** **[v2.8: LIVE AND PROVEN: Dan's re-creation caught as MK-00000001, and the purge deleted him cleanly BECAUSE the record enumerated him; the record is also what makes the purge protocol lawful.]**
- **WHERE AN INSTRUCTION HAS FAILED TWICE, CHANGE THE STRUCTURE SO THE WRONG ANSWER CANNOT BE EXPRESSED.** **THE REFUSAL DOCTRINE:** a law that only permits corrections it can prove safe will sometimes refuse one that is safe, and paying that cost is what makes the law worth having. **DIAGNOSE BEFORE REWORDING: repairing a mass failure without knowing which mechanism produced it is how a tuned fix replaces a tuned defect.** The a23 diagnosis is the doctrine's first full exercise and it relocated the defect before a single word moved. **[v2.7: the 59-callout diagnosis is its second exercise: the defect relocated from "the checkers are bloated" to "one stage doing a deterministic job by inference" before anything was rebuilt.]**
- **THE ITERATION LOOP** (Matthew, 6 August): the inbox is the channel; row export, timings, governors, full-universe regression disposition with every run or it does not count; design verifies per the verification sheet; Matthew grades judgment, inverted (**THE BLIND KEY IS RETIRED**; output first; blank rows the only miss channel; findings state their nature, never a percentage); design defaults technical calls with both positions recorded. **Rulings travel through the inbox and as files; corrections into documents, never chat; wrong text marked wrong in place, never deleted; one structural decision at a time, options with costs, Matthew's calls left open.**
- **CEILINGS ARE PRINTED OR THEY ARE DISCOVERED IN PRODUCTION:** the 120-second cumulative transaction law; governor consumption per stage with an 80% marker; **a suite that cannot see a component is green about nothing; a required reason field satisfied by a non-reason fails loudly, never fills.**
- **THE OPPORTUNITY GRAIN LEADS; PRECISION BEFORE COVERAGE, per grain.** **THE ACCOUNT GRAIN IS BACKBURNERED, NEVER FORGOTTEN.** **DREAMFORCE IS NEVER A DECISION FACTOR.** **Agentforce is not part of this build.** No leads. Behaviour establishes, titles never matter in either direction (the designator title rung resolves IDENTITY, never establishes a role from a title; the two uses stay distinct); one quote, several establishments; advocacy is never discounted for coming from a professional; the families are not equally heavy. **NF1 is written law. Byte-exact propositions and spans. Citations on Answer rows only. No dismiss anywhere. Human override absolute. No decay. No em dashes in written output** (source evidence bytes keep their own punctuation verbatim; the evidence law outranks the style law for evidence).

- **EXISTENCE AND VISIBILITY ARE TWO FACTS AND ARE MEASURED SEPARATELY (15 August, ninetieth stamp).** A schema claim names the org id the query ran against, the API it ran through, and whether that API enforces FLS, or it is not a claim. **Metadata API deploy grants field-level security to no profile and no permission set, System Administrator included:** a field lands PRESENT AND INVISIBLE unless `fieldPermissions` land with it. Design's MCP connection reads through the standard API and is FLS-aware, so **it can never establish that a field does not exist** — only that this user cannot see it; every design describe is reported as a VISIBILITY measurement, and existence questions go to CODE's Tooling read. Proven the day it was named: eight anchor fields on `AAO_Shadow_Person__c` were reported absent by three FLS-aware instruments while present in the org since `12:42:24Z`. The control was `AAO_Shadow_Key__c`, changed in the same deploy at the same second, `required=true` and therefore FLS-unrestrictable, which never left view. **This supersedes the eighty-ninth stamp's `created=false` law whole** — that law rested on a false example and its prescribed remedy is exactly what produced the wrong answer.
- **THREE READINGS OF ONE INSTRUMENT CLASS ARE ONE READING (15 August, ninetieth stamp).** Corroboration requires instruments that FAIL DIFFERENTLY, and **independence is asserted only after it is established** — the capability law turned on our own measurements. Agreement between instruments sharing a blind spot is the blind spot repeating itself, never evidence. Design's error the day it was named: Apex compile, `sf sobject describe`, and `FieldDefinition` through the standard API were reported as three independent instruments agreeing; all three enforce FLS, and they were one instrument wearing three faces.

## 4 · Open · with owners

**[v2.9: THE ACCURACY PHASE GOVERNS THIS SECTION. Current order: the accuracy phase (span set, downsize comparison, s1 and a23 runs, the stability probe with the shard driven, Matthew's grading sheet) → the admission filter proposal → the single-projection proposal → the receipt object → the concurrency measurement → the queue resumes. Execution sessions follow the stamps, PARK structural questions, never redesign, and never rewrite this Board.]**

**Matthew, in order of what they block:**

1. **THE BUYER-ROLE COLLISION, new, methodology, yours alone:** when two buyer roles both verify on one person (h1's live case: Adam established Evaluator AND User; the vendor field holds one; the writer held the field and wrote the refusal note). Options with costs: recency on the evidence-occurred clock (cheap, same-call ties still hold); an authored precedence ladder (which role outranks which is a methodology statement); hold plus a flag for the human pick (zero risk, one more flag). Hold-and-note stands until you rule.
2. **GRADE tg1's MAP AGAINST PASS 1's at the accuracy phase.** The runs disagree on identical bytes (Adam three ways across three runs; Dan's polarity; 59, 44, 41 pairs). At most one map is right; this grading is the accuracy phase's human half, the stability probe is its mechanical half, and the two-read shape is unproven until both land.
3. **The MENTOR siblings, poq5/poq6, and buq2** stand as ruled at the twenty-fifth stamp, veto open, builds waiting behind the accuracy phase.
4. **The production transcripts as files, three or more, MIXED STAGES, one as foreign as possible.** Load-bearing: the only positive foreign measurement, and the only instrument that separates stage from generalisation. Project Farma was the first; more stand owed.
5. **THE BOARD'S REWRITE-WHOLE CONVENTION, still open with costs** (keep-as-is with the re-read procedure; make it additive; or split into a state header plus append-only ledger). Design's default, recorded and not acted on: the third. The execution lane's never-rewrite rule is an interim mitigation, not the answer. Yours.
6. **CR-00000000's vendor projection on the Emerson deal still displays the word CRITERION**, marked and reported, never deleted by us. Delete it by hand or leave it; one row, your call.
7. The empty-license gate ruling; poq7/seq2 standing; the Adam Meloan re-read; the Toby deck.

**CODE (fresh session; seed = read the inbox top stamps, thirty-second down through twenty-seventh):** **THE ACCURACY PHASE**: the span set (twenty-ninth stamp item 1, caps proposed before build), the call 3 downsize comparison (adjudicated, smallest model that passes on the current claim shape), the two-read shape against s1 and a23 with full exports, **the STABILITY PROBE** (three identical runs on frozen PF, purge between, mechanical diff on the merge key, STABLE versus FLICKERING per establishment, shardCount=2 driven inside so the 50-stretch number falls out free), the batch lane's token number measured with cache_read live, and the exports for Matthew's tg1-versus-Pass-1 sheet (criterion names and internal marking derived, never by hand). **Then, in order: the admission filter proposal, the single-projection proposal, the receipt object (with or immediately after, your call, reported), the express-lane admission-drop semantics with the lane flag, the concurrency measurement.** **Proposals owed before building, unchanged:** the deq3/deq4 dependency defect; the 90-day re-derivation mechanism; the hot/cold split design. **Standing debts:** regression temporal validity, the domain list's seed-metadata home at packaging, `AAO_Internal__c = false` on `wendy.higley@altify.com`.

**Design (execution sessions, lesser models expected, per the working mode and the section 7 seed):** verify every CODE report per the verification sheet and the looks-right standard, spot-checking the org after the Organization query; report verdicts to Matthew plainly, measured never asserted; build Matthew's tg1-versus-Pass-1 grading sheet from the exports; cut additive inbox stamps that ratify, name defects, and state the next queued item, never a new ruling; PARK structural questions; NEVER rewrite this Board or any law document. **Design (strong sessions, short and rare):** rule the parked list; **THE DEEP FOLDS, deliberately deferred and batched with the span-set build so the charter text moves once:** Charters (§P8 supersede marks: two comprehensive reads, the resolution stage, the lanes; Part II call 0 slim), Architecture, Model & Flow (pair ledger, span set, receipt, create-leg record entities), and the Glossary (owed terms: TWO COMPREHENSIVE READS, THE RESOLUTION STAGE, TYPED RESOLUTION REQUEST, SPAN SET, ADMISSION FILTER, OBJECT-BUDGET, LOOKS-RIGHT STANDARD, LANE, STABILITY PROBE, plus the earlier debts: NEGATIVE CONTROL, counting statuses, the tuned-behaviour law, DESIGNATOR, INTERNAL as a grade); regenerate the machine read sheet on any shape change; the negative-control grading-instrument question before the Handle run; the Flags and Guidance fold; the Problems charter after People's gates, with the re-derived cost model in hand.

**Parked, structurally unanswerable here:** module-licence detection; package-upgrade behavior of subscriber edits; provider batch endpoints; the occurred-window and role-map verification (pilot).

## 5 · Next steps, in order

1. **The accuracy phase (CODE):** the span set; the downsize comparison; s1 and a23 runs with full exports; **the stability probe** (three runs, shard driven); the batch-lane token measurement; the exports for the grading sheet.
2. **Matthew:** rule the buyer-role collision; **grade tg1 against Pass 1** from the prepared sheet; the Emerson CRITERION row by hand whenever.
3. **The admission filter proposal** (before any automated funnel), **the single-projection proposal**, **the receipt object**, **the express-lane admission-drop build**, **the concurrency measurement** (the two unverified ceilings: org async concurrency, gateway rate limits).
4. **The next strong session:** rule the parked list; the deep folds (Charters, Architecture, Model & Flow, Glossary) batched with the span-set build; Board to v3.0.
5. **The freeze lifts**: the absent-versus-too-narrow report, the additive propositions (political membership-without-authority, the MENTOR siblings), Project Farma Pass 2 with the designator acceptance table, the foreign fixtures (Handle negative control), further mixed-stage production transcripts.
6. Gates pass; **People closes; PROBLEMS opens** (Goal, Pressure, Obstacle, Initiative; charter design-side first), with the cost model in hand. **People does not close on a23's numbers and nobody should pretend otherwise.**

## 6 · Standing hazards · the complete list

**THE TUNED-BEHAVIOUR HAZARD, 8 August, the largest on this list:** every measurement taken on `ec8e7170` is a calibration and not a result, and the project cannot tell from inside that fixture which of its behaviours are design and which are tuning. a23 proved it: 91% correct refusals became 0%, **and the diagnosis located the tuning in the contract propositions, which the verifier applied faithfully.** **Every repair verified only on the training set re-opens on the first unseen fixture that exercises it**; buq6/EVALUATOR, recorded repaired at v0.6, is the first confirmed re-opening.

**THE STABILITY HAZARD, v2.8, widened at v2.9, the accuracy phase's subject:** the sweep and the two-read shape produced different maps from identical bytes, and then the two-read shape produced different maps from ITSELF across two runs (59, 44, 41 pairs; Adam three ways; Dan's polarity). A single run's map is one draw until the stability probe says otherwise, and a map that changes on rerun without new evidence is its own product problem. Until Matthew grades and the probe reports, no shape's judgment is proven and the looks-right standard deliberately does not claim otherwise.

**THE STAGE-CONFOUND HAZARD, 8 August:** a single fixture is one deal stage speaking one register, so a mass failure on it cannot say whether the defect is general or register-specific. An unknown share of a23's 0-of-18 is stage. Repairs tuned against one fixture's register are the training-set hazard re-entering through the repair door. Mixed-stage transcripts are the instrument.

**THE CONCURRENT-WRITE HAZARD, 8 August and demonstrated the day it was named:** more than one design session can hold this document set at once, there is no version token on write, and **a full-file replace from a stale read destroys a concurrent fold and reports success.** Mitigation is procedural: re-read by exact path immediately before writing, add rather than reconstruct. **A state assertion in this Board is exactly as trustworthy as the freshness of the read behind it.**

**THE EXECUTION-DRIFT HAZARD, v2.8:** an execution session that redesigns mid-loop reintroduces every relitigating failure this document set exists to prevent, on whatever model it runs. The working mode is the mitigation: park, never answer; the stamps are law; strong sessions rule the parked list only.

**THE MOVED-COST HAZARD, v2.7:** the resolution rebuild moves cost from model callouts to org queries and DML; a cost moved and not measured is how the next fifty-nine hides. The gate printed it (7 s of org time against the ~59 model seconds deleted); every run prints it from here.

**THE INSTRUMENT HAZARD, 8 August:** the grading sheet is an output of the system and inherits its blind spots. Two proven instances on a23.

**THE ROW-DEPENDENCY BLIND SPOT, 8 August:** call 3 verifies each row blind and independently, so it can uphold a property while refusing its subject on identical bytes. deq3/deq4 is the specimen; the span set narrows the starvation half of this; the dependency half stays an open proposal.

**THE WRONG-REFERENT HAZARD, with the designator ruling:** a pronoun resolution is a model judgment the byte check cannot verify. Mitigation structural: the introducing span is quoted and byte-located, titles and pronouns can link but never create, AMBIGUOUS never guesses.

The only queryable orgs are Altify's own; ~80% of installs have none of its history (**the hazard stands until pilots answer it**). **THE TRAINING-SET HAZARD:** a calibration fixture's precision predicts nothing; unseen speech is the only real number. **THE ATTENTION HAZARD**, two named modes: the cheap whole-family blank and the within-read miss **[v2.7: the two-read union is the new instrument against the within-read miss; measured at the accuracy rerun, never assumed]**. **THE SILENT-SUCCESS HAZARD**, extended four times: a stage reporting success while writing nothing; a rule with a reporting half and no enforcement half; a write that skips its own watermark; a document write that succeeds by destroying another's. **THE SILENT-DROP HAZARD:** repaired by the re-entrant resolver and the designator ruling; Rich's held pairs at the gate are the repair working. **THE ASR-NAME HAZARD:** a mentioned name is what a transcriber heard; a Contact minted from a transcript spelling carries the error permanently; the byte check cannot help. **THE GREEN-SUITE HAZARD.** **THE KNOWN-RULE-UNAPPLIED shape.** **THE STAMP-LEDGER SPLIT.** **THE CORRECTION THAT DISABLES WHAT IT CORRECTS.** **THE RATE HAZARD:** never gate on a ratio whose denominator is the thing being improved. **THE THIRD-STATE HAZARD.** **THE SPAN-BOUNDARY HAZARD** (deq1/deq2, THIN; the span set is its repair, accuracy phase). **THE BYTE-CHECK BLIND SPOT:** invented words are caught, shortened ones are not. Retrieval returns chunks from retired versions: open by path, demand stamps. ECI's related-record stamp is inference. Vocabulary: our answer row; verified, never scored; receipts, never confidence; Agentforce-ready, never Agentforce-dependent. `CreatedBy` cannot separate CODE from Matthew. FLS is a second fact beside deploy success. Lightning caches hard. A run that succeeds only on a cache hit is not a run that works (and every timing run labels COLD or WARM for exactly this reason). The unit, display, off-badge, run-date, decision-slot, and intake hazards. **The scale plane is honestly parked, not solved.**

**THE TREE-AND-ORG HAZARD, 15 August, and it caught the session that named it:** two source trees live on the build device — `Downloads/claude` (`main`, `aao-build`), which carries the live schema, and `Downloads/aao-sandbox` (`master`, `aao-sandbox`), which carries none of it and holds the shell's default working directory — while the global `sf` default target org is `altify-dev` `00Dg500000B0KjZEAV`, which does not contain our objects at all. Neither project set its own target. `target-org=aossb2` is now pinned in the live tree only; the pre-loss tree stays untouched, config included, as design's evidence. **Every deploy and every org query names its tree and its org id in the report.** CODE hit this live while writing the report that named it: a persisted `cd` made it read the backup's ledger believing it was the live one, caught only by byte count. **Absolute paths, always.**


## 7 · The session seed

```
Seed - AAO EXECUTION session, lesser model expected. Focus: THE
ACCURACY PHASE, receiving and verifying CODE's reports, per inbox
stamps 27 through 32. Cowork session attached to the Altify
Agency project.

YOUR LANE, absolute: you verify, relay, prepare grading sheets,
and park. You do NOT redesign, do NOT relitigate any ruling, do
NOT touch Charters, Architecture, Model & Flow, or the Glossary,
and you NEVER rewrite this Board (strong sessions only; a stale
full-file write destroys concurrent work silently). Your only
writes: ADDITIVE stamps on CODE-INBOX.md (re-read it immediately
before writing), entries under its "For the next design session"
list, and grading-sheet satellites as new files. If a decision
feels structural, it IS structural: park it with options and
costs, tell Matthew a strong session should rule it, and continue
on the stamped path.

Read first: this Board (the v2.9 stamp governs), then
CODE-INBOX.md stamps thirty-second down through twenty-seventh
(the active law), then aao-verification-sheet.md, then
aao-machine-read-sheet-v1_0.md (internally v1.1). Open by exact
path, never search. Numbers only from CODE's BUILD_JOURNAL or its
run reports. Before ANY org work: query Organization first;
sandbox is altify--aossb2 (00DWD00000DV7iT2AT); PRODUCTION
(altify.my.salesforce.com, 00DHn000006jYatMAE) IS READ-ONLY
UNCONDITIONALLY, and altify-pbo is never read.

State on pickup: the pipeline is rebuilt, gated, and halved
(pf0808-h1: ~57 s in-org COLD, 6 model calls, worst callout 27 s,
ratified; commit 5b8b3d1). The active work is THE ACCURACY PHASE:
CODE builds the span set and the call 3 downsize comparison, runs
the two-read shape against s1 and a23, runs the STABILITY PROBE
(three identical runs on frozen PF, purge between, mechanical
diff on the merge key, shardCount=2 driven inside), measures the
batch lane's token number, and ships full exports.

WHEN CODE'S REPORT ARRIVES:
1. Verify per the verification sheet and the looks-right
standard: artifact sha against the freeze list; bytes exact;
ledger arithmetic (located = disposed; verdicts = identified);
traps zero (a fictional person anywhere on a map = failed run,
whatever else passed); timings with COLD or WARM labels; no
per-pair callouts; worst callout under 90 s. Spot-check the org
with two or three SOQL queries where the report claims map
values, after the Organization check.
2. Report the verdict to Matthew plainly: measured, never
asserted; name what you verified yourself and what you took on
report.
3. Build Matthew's grading sheet for tg1 against Pass 1 from the
exports: every establishment with its verbatim quote, criterion
NAMES shown, internal people MARKED, both derived, never by
hand; blank rows are his only miss channel. The stability
probe's STABLE/FLICKERING lines ride the same sheet as context,
mechanical, ungraded.
4. Cut the next inbox stamp: ratify what passed, name defects
found, state the next queued item FROM THE STAMPED ORDER (the
admission filter proposal, the single-projection proposal, the
receipt object, the express-lane admission-drop build, the
concurrency measurement). No new rulings in stamps, ever.
5. NOTHING COUNTS AS ACCURACY until Matthew grades. The probe is
never tuned against on its first output.

Open to Matthew (remind him, never decide for him): the
buyer-role collision (recency, precedence ladder, or
hold-plus-flag); grading tg1 against Pass 1 when the sheet is
ready; the Emerson CRITERION vendor row, by hand.

Rules: corrections into documents, never chat; wrong text marked
wrong in place, never deleted; a report without its rows does not
count; row export, timings, governors with every run; the
counting laws; the stage caveat and tuned-behaviour law ride
every single-fixture finding; no em dashes in written output. At
session end: append your state to your own inbox stamp; do NOT
rewrite this Board; list anything parked and anything owed.
```

---

*End v2.9. This document is rewritten at every strong-session close and carries everything; prior versions do not exist. Execution sessions never rewrite it. Whether the rewrite-whole convention survives concurrency is section 4's open decision.*
