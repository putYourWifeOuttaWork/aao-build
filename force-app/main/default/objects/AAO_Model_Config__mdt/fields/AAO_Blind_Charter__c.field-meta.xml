<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>AAO_Blind_Charter__c</fullName>
    <description>Written by: a human, in this record. Read by: AAO_Extract.review. The second reader is a separate charter from extraction and carries its own name, because the two are versioned independently and a run has to be attributable to both.</description>
    <externalId>false</externalId>
    <fieldManageability>DeveloperControlled</fieldManageability>
    <inlineHelpText>Written by: a human, in this record. Read by: AAO_Extract.review. The second reader is a separate charter from extraction and carries its own name, because the two are versioned independently and a run has to be attributable to both.</inlineHelpText>
    <label>BlindCharter</label>
    <length>80</length>
    <required>true</required>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
