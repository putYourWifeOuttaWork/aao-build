<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>AAO_Bind_Group_Size__c</fullName>
    <description>How many bind items ride in one callout. MEASURED, never assumed: ruling 6 of the 3 August inbox. Time one small call, then choose. Lives in config rather than Apex because the next measurement will move it and a constant in Apex would make a measured number look like a design decision.</description>
    <externalId>false</externalId>
    <fieldManageability>DeveloperControlled</fieldManageability>
    <inlineHelpText>How many bind items ride in one callout. MEASURED, never assumed: ruling 6 of the 3 August inbox. Time one small call, then choose. Lives in config rather than Apex because the next measurement will move it and a constant in Apex would make a measured number look like a design decision.</inlineHelpText>
    <label>BindGroupSize</label>
    <precision>4</precision>
    <required>false</required>
    <scale>0</scale>
    <type>Number</type>
    <unique>false</unique>
</CustomField>
