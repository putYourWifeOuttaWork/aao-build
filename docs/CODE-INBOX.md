# CODE inbox · 3 August 2026 · build-day fold · re-stamped last, supersedes all prior inboxes

Sync all five documents into `docs/`, overwriting: Board v1.2 (rewritten whole), Charters v2.3, Architecture v3.2, Model & Flow v1.1, Glossary v2.3. The fixture and transcript files in this zip are unchanged from the last carry; your copies are already verified, sync or skip. Run sheet deliberately absent as always; your v1.3 is newer.

What the fold ratified, all already relayed in-session and now stamped: the writer shape (§P7.2.1), the two-sided guard, the declared applicable set with the org-global read marked wrong in place, membership-never-routes, Unset-null-not-Unknown, your population revert, your FLS finding and the CreatedBy attribution rule (both now Board hazards), the Process half-measure folded into the Board's reconciler umbrella item.

Your queue, in order:
1. §P7.2 steps 3+: Source ingest through the front door (SHA-256 must match the tracked hash), the model pass declaring People only, the AAO_TEMP_ invoker draining projection, the mechanical assertions per the brief. The exclusion-list assertion is the v2.2 restated form: per-dimension stamp isolation.
2. Run report to BUILD_JOURNAL and to Matthew: counts at every stage, assertion results, and the command center live while it runs.
3. Hand the surface to Matthew with receipts. His adjudication is the judgment; his notes come back through the zip.
4. Nothing outside §P7.2 is built. Defects found by the run are fixed and journaled, never redesigned mid-run.
