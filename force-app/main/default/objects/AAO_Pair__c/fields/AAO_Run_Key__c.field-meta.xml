<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>AAO_Run_Key__c</fullName>
    <description>Written by: the pass, once per run. Read by: the one-for-one-for-one arithmetic, which is per run and meaningless across runs, and by the run receipt when that object exists. Carried as text now so the receipt can join later without backfilling a corpus.</description>
    <label>Run Key</label>
    <type>Text</type>
    <length>32</length>
    <caseSensitive>false</caseSensitive>
    <unique>false</unique>
    <externalId>false</externalId>
    <required>true</required>
</CustomField>
