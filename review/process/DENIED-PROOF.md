# The DENIED path, proved at the runtime

**Tree** `/Users/thefinalmachine/Downloads/claude` (`main`) · **Org** `00DWD00000DV7iT2AT`, sandbox `aossb2` · authorized by the hundred-twenty-eighth stamp's item 6. Rows in `review/process/step-b/`.

> *"Ruled: DENIED gets its own targeted proof at the runtime before (c) treats the red-flag family as working, and it does NOT ride call 4. ... A short targeted fixture with one voiced negative, run and journalled, is the whole cost."*

## The fixture

`AAO PROOF - denied and computed`, account `Northwind Proof Co`, opportunity `006WD00000TtfriYAB`. Eleven turns, three speakers: one seller at `altify.com` (internal by the domain split), two buyers at `proofco.example`.

**AUTHORED. MECHANISM ONLY. COUNTS TOWARD NOTHING.** It is not a corpus and no accuracy reading comes off it. It carries two things deliberately and nothing else of interest: one buyer voicing a negative, and one buyer voicing that she makes the final call - the second because the computed catalog's predicates had nothing to fire on anywhere in this org and a mechanism is unverified until tried from the runtime that will exercise it. Two proofs, one run, four callouts.

Wells Fargo is untouched. The rehearsal and gate deals are untouched.

## The result: DENIED fires, end to end, and it is not a FALSE

Two questions came back DENIED, both upheld by the blind verifier with its reason, both standing as establishments in their own right.

**B1 · a budget exists for this purchase.** Gwen Arkwright: *"No. There is no budget allocated for this, not in this fiscal year. Nothing has been set aside and I want to be straight with you about that."* - bytes 494–635, coverage Full.

> call 3, blind: *"The words explicitly state that no budget is allocated for this purchase in the current fiscal year, directly answering whether funds are available."* - **Upheld.**

**A3 · the approval chain is understood.** Same speaker: *"there is nobody above me on it. It is my decision, full stop."* - bytes 354–415.

> call 3, blind: *"The words state there is nobody above the speaker on this decision, directly answering who else must approve."* - **Upheld.**

Both landed as claims and answers:

| contract | verdict | interpretation | basis |
|---|---|---|---|
| `AAO_BANT_B1` | **TRUE** | **DENIED** | Transcript |
| `AAO_BANT_A3` | **TRUE** | **DENIED** | Transcript |

**The clause the stamp attached rides in the rows: DENIED IS NOT A RETURN OF FALSE.** The verdict reads TRUE - the establishment stands, cited to the customer's own affirmative words about the negative - and DENIED is the INTERPRETATION. True-or-nothing is not breached; the voiced no is a fact that was established, and what it establishes is the negative.

Beside them, the same run produced AFFIRMED on six other questions from the same mouths, so the family discriminates within one call rather than reading one way for structural reasons.

## What this proof does NOT establish

- **It does not make the red-flag family work.** Nothing yet reads `AAO_Interpretation__c = 'DENIED'` and raises a flag. That is (c), and (c) is where the two field questions from item 7 get answered. What is now true is that (c) will find real DENIED rows to build on rather than an unexercised branch.
- **It says nothing about rate.** One authored fixture voicing one deliberate negative proves the path carries a DENIED; it does not say how often real speech produces one. Design's own diagnosis at item 5 - that a zero can be corpus or grammar and one run cannot separate them - applies here in the other direction.
- **The run carries one error leg**, mine, in the computed catalog stage (`REQUIRED_FIELD_MISSING: [Account, Outcome]`), fixed and resumed. Under the gate law this is a FAILED run. It is a targeted proof and not a gate run, and the leg stands on the receipt rather than being tidied away.

## Timings

`RCPT-00000053`, run key `demo-005v4000-20260819153337`. Call 0 8,517 ms · read 1 21,927 ms (21 located, COLD) · read 2 16,905 ms (19 located, WARM) · call 3 verify 26,920 ms (21 disposed) · join 1,442 ms (14 claims) · computed catalog 1,136 ms, 0 callouts · projection 1,421 ms · cards 7,862 ms. Four callouts, worst 26,920 ms - **31% of the 88,000 ms measured gateway wall.** No leg self-marked past budget.
