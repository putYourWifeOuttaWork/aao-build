<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>AAO_Raw_SHA256__c</fullName>
    <description>The hash of the RAW INPUT this row was normalized FROM, written by whatever performed the normalization. Ruled 7 August: the normalizer stamp composes, normalizer version plus raw input hash, so two different byte outputs can never carry identical stamps. Null on rows normalized before this field existed, where the stamp falls back to the output hash and says so in its own prefix.</description>
    <externalId>false</externalId>
    <label>Raw SHA256</label>
    <length>64</length>
    <required>false</required>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
