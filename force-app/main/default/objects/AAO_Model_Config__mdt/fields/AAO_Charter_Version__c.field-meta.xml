<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>AAO_Charter_Version__c</fullName>
    <label>CharterVersion</label>
    <description>Written by: a human, in this record, and bumped whenever the prompt or the output schema changes in any way that could change what the model proposes. Read by: AAO_Extract, stamped onto every Candidate. This is the grain abstention analysis groups by, so a silent prompt edit under a fixed version corrupts every rate computed across the boundary.</description>
    <inlineHelpText>Written by: a human, in this record, and bumped whenever the prompt or the output schema changes in any way that could change what the model proposes. Read by: AAO_Extract, stamped onto every Candidate. This is the grain abstention analysis groups by, so a silent prompt edit under a fixed version corrupts every rate computed across the boundary.</inlineHelpText>
    <type>Text</type>
    <length>20</length>
    <required>true</required>
    <unique>false</unique>
</CustomField>
