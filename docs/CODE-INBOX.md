# CODE inbox · re-stamped LAST, 4 August 2026, afternoon · the simplified pass, the rebuild is GO

**Read this, then `docs/aao-board.md` (v1.5), then Charters v2.7 §P8 whole, then the Architecture v3.6 head, then the harness satellite `aao-pass-test-harness-v1_0.md`. The criteria grounding is `aao-decision-criteria-research-v0_1.md`. Numbers from your BUILD_JOURNAL. A v2.6 inbox may have reached you between this one and the morning hold; this inbox supersedes it whole, and §P8.6 in the charters names exactly what v2.7 reversed from v2.6.**

## One focus
**Rebuild the pass to Charters v2.7 §P8 and run the harness.** §P8 is the build spec; the harness satellite is the test spec; the CSV is the deliverable Matthew reviews.

## Your correction, standing
`9a0720a` was journal-only; the unit-law fix in `AAO_EBV.report()` is queued, not shipped. Recorded at the Architecture v3.6 head and corrected in Board v1.5 §2. The fix stays on your list below.

## Rulings since the morning hold, each one line
1. **The charter text is Charters v2.7 §P8, rewritten whole from v2.6 the same day; where anything you hold disagrees with it, §P8 wins.**
2. **No abstention rows exist, at any grain, ever; the v2.6 vocabulary (`not_surfaced`, `not_in_excerpts`) is retired unbuilt; do not build any silence-writing machinery.**
3. **Auditability is the run receipt: one row per Source per pass (read when, charter versions, calls made, pairs found, claims written); plus a query-proof that zero abstention rows exist is itself a harness test.**
4. **One for one, for one: every pair gets exactly one call-2 disposition and, if identified, one call-3 verdict; enforced as arithmetic between row sets; mismatch throws as our bug, per the parse law.**
5. **Call 1 emits question-answer pairs: question code, verbatim words (byte-located, exact match; zero matches discards and counts; multiple matches all stand), voiced meaning, full-or-partial on fact families only; NO person on any pair.**
6. **Call 2 identifies only: per pair, one of a closed-list person, NONE, or AMBIGUOUS, with a one-sentence basis; it never re-judges a pair, never reads the whole artifact, never invents an identity; AMBIGUOUS routes to the Identification flag.**
7. **Call 3 verifies blind, unchanged in kind; its small-model comparison gate re-runs on the new claim shape; sentiment claims add the scope check (the feeling must be toward us).**
8. **True-or-nothing: no FALSE, no inversion, no two-sided verdict anywhere in the pass; the two-sided Political questions split into one-way halves, each pointing at its own placement.**
9. **The Support quiz is retired from extraction; sentiment is read directly into five states with one-line label meanings; the counter is Support's mechanism: one step per call per person toward the voiced state, never past it; mixed nets to no move with all quotes journalled; an explicit declaration of a state jumps to it and flags the sequence when polarity reverses; replay over claims reconstructs the counter exactly.**
10. **The pair ledger: initial unowned row (call 1) plus linked identified row (call 2); the initial row's verbatim string is the anchor; no separate anchor entity. Propose the mechanism as one line back through the zip before building; the entity ruling then goes to Model & Flow.**
11. **Resolution windows candidates on the evidence-occurred clock: open plus recently closed as of the call datetime, never the run date. The 17 June fixture proves this or the harness fails T1.**
12. **The criteria contracts are §P8.5 (DC-N, DC-F by provenance, DC-R by gate language), voicer-only junctions, query-then-branch on the junction (it accepts duplicates, measured in prod).**
13. **Questions once per transcript; cost scales with transcript length; splits by pair or claim batch on measured thresholds; no call retries by growing; every counter unit-labeled; report thinking share per call (the rumination check).**
14. **The old Emerson contracts blocker is treated as dissolved (contracts are no longer person-crossed); Matthew confirms; the 29 July video stays blind regardless, key owed and unspent — no output on that fixture exists until the key does.**

## Your work, in order
1. **The watermark question** (decays; blocks any second pass on B&V): did the manual run write `AAO_Projected_Value__c`/`AAO_Projected_Modstamp__c`; what does command-center §6 count and why `off`.
2. **D1/D2 and the unit-law fix** in `AAO_EBV.report()`.
3. **The cheap count** (16 established items, projecting versus placing-nobody).
4. **Pair-ledger mechanism proposal**, one line back.
5. **Normalization intake per the harness §2–T0:** format declaration, annotation stripping to sidecars, NF1 out, determinism by hash, convergence diff between intake paths. **Plus the Path A capability test: call the Get Conversation Transcript action from Apex and journal the result either way; it has never been tried from the calling runtime.**
6. **Call 1, then the 24 June recall gate** (targets: the four graded PASS spans, the S2/Adam placement words, the canonical criterion; assert the two graded over-reads do not reproduce). Gate passes before anything downstream is trusted.
7. **Call 2, call 3's re-comparison, arithmetic (T5), projection preview (T6), receipts (T7).**
8. **The Emerson harness end to end** on the hand-carried 17 June artifact; seed Emerson per the §P7.2 seeder law (harness §5); emit the CSV exactly per harness §4 and return it through the zip.
9. Journal every measured constant with its measurement.

## Standing, unchanged
Production read-only unconditionally. LAW #1. Nothing of ours on native or ALTF objects. Capability claims unverified until tried from the calling runtime. Expected failure mode: over-reading, never fabrication; the new watch item is sentiment over-reading under the looser standard, which call 3's scope check and Matthew's CSV grades measure.

## Sequence out
Your rebuild with the 24 June gate → the Emerson harness and CSV → Matthew and design grade it together (the grades become the fixture's answer key and calibrate the warmth bar) → Matthew's blind key → the 29 July fixture runs blind → adjudication → fold → resolver build (Matthew's hard law: resolver next, before Politics).
