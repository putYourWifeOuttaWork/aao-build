<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>AAO_Stored_Value__c</fullName>
    <description>The value as the vendor picklist STORES it, which is the only thing our code ever reads or writes. Must match the picklist's stored value exactly; the displayed label is a separate field precisely because the two can differ.</description>
    <label>Stored Value</label>
    <type>Text</type>
    <length>80</length>
    <required>true</required>
    <unique>false</unique>
</CustomField>
