<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>AAO_Org_Override__c</fullName>
    <description>THE ORG&apos;S answer, and null until somebody sets it. SubscriberControlled, so an upgrade never touches it and we cannot overwrite what a customer chose. A Checkbox cannot express null, so the override is a picklist and blank means untouched: without that, a shipped default of ON could never be turned off by a customer who had simply never opened the record.</description>
    <fieldManageability>SubscriberControlled</fieldManageability>
    <label>Org Override</label>
    <required>false</required>
    <type>Picklist</type>
    <valueSet>
        <restricted>true</restricted>
        <valueSetDefinition>
            <sorted>false</sorted>
            <value>
                <fullName>On</fullName>
                <default>false</default>
                <label>On</label>
            </value>
            <value>
                <fullName>Off</fullName>
                <default>false</default>
                <label>Off</label>
            </value>
        </valueSetDefinition>
    </valueSet>
</CustomField>
