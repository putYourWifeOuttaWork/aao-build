<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>AAO_Target_Id__c</fullName>
    <description>The 18-character Id of the org record the machine created. A text field rather than a lookup on purpose: a lookup to Contact would null out if an admin merged or removed the row, and this record must keep saying what we created even after the target is gone.</description>
    <label>Target Id</label>
    <type>Text</type>
    <length>18</length>
    <caseSensitive>false</caseSensitive>
    <unique>false</unique>
    <externalId>true</externalId>
    <required>true</required>
</CustomField>
