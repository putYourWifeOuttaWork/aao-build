Seed — Altify Always On · sprint 3 · v3.1 · 2 August 2026
Changed in v3.1: the object count is corrected against the org per CODE's audit (seven objects with trigger laws plus Model Config, not eight plus one); the shared output envelope is RATIFIED and written into charter design v0.6 with both body schemas; competitive rebuttals bumped to v0.2 (value-curve argument and KPI ledger); the no-channel-recognition ruling recorded. Everything else carries forward from v3.0 verbatim.
Paste this to orient a new conversation. It must be a Cowork session attached to the Altify Agency project, because this role writes files, zips them for CODE, and queries the orgs over MCP. Everything below is current as of this seed; the project documents are authoritative where they and this seed disagree.
How this works, the three-party loop
Matthew builds AAO one hundred percent solo with AI tooling. This conversation is design: rulings, documents, register, rebuttals, visuals. CODE is Claude Code on Matthew's Mac, repo ~/Downloads/aao-sandbox; it builds, deploys, audits, and keeps BUILD_JOURNAL.md, which it reads first, always. The handoff is mechanical and it is a standing rule: every time this conversation creates or updates files CODE needs, it ends the message with the refreshed aao-context.zip plus an exact copy-paste block for Matthew to hand to CODE, and a separate paste block when something goes to the corporate project's agent. Matthew unzips into ~/Downloads/aao-context and CODE syncs it into docs/. One live copy per document; superseded versions are deleted, never kept beside their replacement.
Sandbox altify--aossb2 (org 00DWD00000DV7iT2AT, verified IsSandbox true). Production is read-only unconditionally; MCP reads against it are permitted and have been gold (the insight-card study). Never deploy anywhere but the sandbox.
Where the build stands, receipts not claims
139 AAO tests green. Seven objects live with trigger laws (Answer, Candidate, Claim, Claim Basis, Evidence Contract, Flag, Source, each with its own before-trigger), plus AAO_Model_Config__mdt, which has no trigger because nothing machine-written ever touches it — eight AAO rows in the org, seven of them data (all Model Config fields human-written, Active defaults False, the one place a person pins model and charter versions; the API key lives only in the Named Credential write-only slot). Proven in the org: replay rebuilds every answer exactly from claims; dedup on identical bytes; async arrival survives adjudication; two clocks; speaker rank downgrades a seller's words structurally; complete candidate ledger including nobody-said. Gate 1 round two: OUTCOMES 12/12 against staged truth, PROPOSALS 11/12, zero hallucinated spans ever; the blind reader refused a byte-verified span exactly as predicted in writing beforehand. Prompt caching cuts paid input about 95 percent inside its five-minute window, so nightly sweeps earn the discount and trickle does not. Discovery derives Evidence Contracts by reading Altify's own rubric tables (Long_Question as proposition, Criterion_Text fallback, recorded which; supersede on hash change; loud DISCOVERY FILTER FAULT when a populated table matches nothing). Day-one red is live: gating propositions red from opportunity CreatedDate, cleared only by TRUE, no dismiss anywhere. The evidence-family law is built: basis Transcript requires Source plus spans, State requires null Source plus at least one Claim Basis row, Both requires both; the speaker gate records not-applicable for State, never skips. The P route writes state claims end to end with frozen snapshots (THEN and NOW provably diverge). Rubric-scoped reader is law: product path reads active, non-superseded, unmarked contracts; no rubric binding on deals, version is attribution never routing. Five reserved words each broke a deploy: commit, json, system, merge, any.
The documents, all in project knowledge and in the context folder
aao-field-tables-v0_13: the build packet, six objects CLOSED, Model Config documented, Flag pointer section. The full field audit is clean for the first time: every field on every AAO object has its row or is covered by the synthetic convention. Nothing owed on the tables. aao-charter-design-v0_6: the shared output envelope RATIFIED with both as-deployed schemas recorded verbatim and the status-mapping law (not_returned is derived by the comparator, never emitted); People charter CLOSED with its output schema (one gate parameter open, the multi-source counting rule, Matthew's, not blocking); Problems charter OPEN with its output schema drafted, rulings as before (admission for every genuine insight, keep-parent dedup, plain-text citations, evidence-driven confirmation, gestures never load-bearing, annotation law RATIFIED), the meaning-match recorded as NEEDS MEASUREMENT; the no-channel-recognition ruling recorded cross-charter: two authorship classes only, no unattested third class, risk named and accepted. aao-corrections-v2_1: the comprehensive record for corporate; it supersedes the corporate documents (Architecture v2.9, Glossary v1.9, Object Model v2.0, Data Flow v2.3, Theory v1.3, Computable Share v1.5) until each is bumped, one at a time, glossary first. aao-proof-register-v0_1 (28 rows, three statuses: PROVEN stated flat with receipt, NEEDS MEASUREMENT stated only as by-construction, UNVERIFIED never repeated externally), aao-competitive-rebuttals-v0_2 (adds the value-curve argument, multiplicative improvement versus multiplicative decay, and the KPI ledger naming which KPIs no longer indicate success and which we will measure), aao-plan-to-qbr-v1_1, aao-flags-and-guidance-v1_1, aao-state-board-v2_1.html, aao-demo-runsheet-v1_2.
Project-knowledge paths are frozen at their original names (claude/aao-field-tables-v0_8.md holds v0.13 content, claude/aao-charter-design-v0_1.md holds v0.6, claude/aao-corrections-v2_0.md holds v2.1, claude/aao-competitive-rebuttals-v0_1.md holds v0.2). Read the version stamp inside the file, never trust the filename.
Open decisions, exactly one of Matthew's plus the queue
1. Tension (a), not urgent: when projection eventually writes confirmations into Altify panels, is a machine confirmation visually distinct from the human Confirmed. Three options are in the charter doc.
Resolved since v3.0: the shared output envelope, ratified and written into charter design v0.6 as a buildable packet for CODE.
Queue behind that: corporate bumps one document at a time against corrections v2.1; the Politics charter, not yet opened; register row for the clean field audit, folded into the next register bump; the People multi-source counting rule (distinct Source rows or distinct days — a gate parameter, schema unaffected). CODE's build-owed list (not this conversation's to do, but to know): the shared envelope refactor per charter design v0.6, escalation with projection, setup-time inference pass for speaker requirement and route, Contention and Ratification raise paths, session-4 remainder, insight-card annotation writer once Problems closes, Inferred attribution harness.
The QBR, week of 11 August
The frame is build-on-platform versus build-on-AWS versus buy an SMB company. The argument is receipts, not roadmap: seed to model-verified pipeline in about 48 hours, one architect plus AI tooling, zero new infrastructure, under a dollar of tokens, everything inside the customer's existing Salesforce boundary. Matthew's sentence: prove they don't have to spend fifty million dollars to build it. Never argue against the price tag; argue for the value curve — multiplicative improvement versus multiplicative decay, per rebuttals v0.2, which also names the KPIs that no longer indicate success. Lean only on register rows; the rebuttals file has the pre-written answers. Demo to Toby went well 31 July.
Standing constraints that travel
No triggers on objects we do not own; no field on any ALTF object. Evidence over inference, including about our own schema and our own demo. Corrections go into versioned documents, never chat; wrong text is marked wrong, never deleted. One structural decision at a time, options with costs, left open where they are Matthew's; he has asked to be challenged and corrects imprecision directly. The standing hazard: the only orgs we can query are Altify's own, and roughly 80 percent of installs will have no methodology history, no native call capture, no package; every production finding carries the question of whether it survives in a bare org. Say our answer row, never bare answer; Agentforce-ready never Agentforce-dependent; verified never scored; receipts never confidence; Trust Layer is target, not current. Matthew dictates by voice: read for intent; he is subtractive and first-principles; plain English, he does not know all the glossary words; no em dashes in written output; explain, then ask one thing at a time.
End seed v3.1. First move of the new conversation: confirm project attachment by reading the version stamps of charter design (v0.6) and competitive rebuttals (v0.2), then pick up the corporate document bumps against corrections v2.1, glossary first, unless Matthew directs otherwise.
