# AAO Proof Register — living file

**v0.1 · 31 July 2026**
Discipline: three statuses. **PROVEN** carries the receipt and may be stated flat. **NEEDS MEASUREMENT** names its test and is stated only as a design property ("by construction"). **UNVERIFIED** is never repeated externally. A claim moves up only with a receipt; nothing moves silently.

## PROVEN in the org, receipt named

1. **Incrementalism across evidence.** UNVERIFIED with receipts after call one, TRUE after call two, first claim untouched forever. Receipt: live run 31 Jul, claims CLM-00000008/9; exit test green.
2. **Replay.** Answers deleted and rebuilt from claims exactly, field for field. Receipt: in-org REPLAY exact=true on all three demo deals.
3. **Same evidence twice produces nothing.** Duplicate ingest enqueued zero jobs, live on camera, twice. Receipt: terminal transcripts 31 Jul.
4. **Two clocks / backfill.** June evidence processed in July, ordered by occurred, not by processing. Receipt: status output, claims 11 days apart occurred, seconds apart recorded.
5. **Arrival survives adjudication.** Trigger and Queueable are separate transactions; the artifact lands even if judging fails. Receipt: AAO_LiveIngestTest asserts zero claims before stopTest.
6. **Zero dependency on Altify.** Full pipeline on an opportunity with no Altify record; all fourteen ALTF rubric tables present and empty. Receipt: describe 31 Jul; demo deals.
7. **Speaker rank enforced structurally.** Identical words from the seller land UNVERIFIED, outcome Downgraded, with full element coverage. Receipt: seller-said-it deal.
8. **Complete consideration ledger.** Every proposition gets a candidate row including nobody-said, so "did it even look" is answerable. Receipt: candidates=6 per pass in org.
9. **Write-blocking customer constraints are real.** The sandbox's own pre-existing test fails on a customer validation rule. Receipt: ConvertToOpportunityTest failure, journal.
10. **Secret handling.** API key in an encrypted write-only slot, merged after Apex builds the request, unreadable from code, absent from debug logs. Receipt: credential deploy 31 Jul, journal, verbatim platform messages.
11. **Gate 1 round two, first live model run end to end.** claude-opus-5 through the Named Credential, both transcripts. **AGREE 11, DIFFER 1.** Zero hallucinated spans on both passes (spansDropped=0 twice). All ten abstentions correct (nobody_said on every non-applicable proposition). Transcript two exact: TRUE, covered e1,e2,e3. Tokens: 3,137 in / 915 out / 13.7s, then 3,099 in / 984 out / 11.4s. Prompt assembled at runtime from Evidence Contract records, every enum built from org data. Receipt: Gate 1 pass T1SRC + T2SRC + compare, 31 Jul 11:48–12:01.
12-s. **SUPERSEDED by rows 12a-12c (1 Aug).** Row 12 recorded the comparison harness catching what the gates upheld, with the absent blind reader named as cause. The blind reader now exists and the gates catch the same specimen without the harness. The row stands as the record of the before-state.
12a. **The prediction held, with the confound stated.** Registered in advance: the blind reader should refuse e3, coverage should drop to partial, the verdict should land UNVERIFIED. It did, exactly. Confound: charter 1.1.0's tightened FALSE bar landed in the same run and independently produces UNVERIFIED (proven by a read-only extraction call), so the verdict outcome is overdetermined. **What is attributable to the blind reader alone is proven from the row, not a log: three spans located and labelled e1, e2, e3, coverage reading missing e3. Only a second reader refusing a byte-verified span produces that combination.** That is the capability no deterministic check has: refusing words that exist but do not establish. Receipt: AAO Gate1 - Blind Reader deal, 1 Aug; register row 19's prediction predates the run.
12b. **Proposals versus outcomes.** The harness graded proposals; graded on adjudicated answers the score is **12/12** (proposals 11/12, and the surviving DIFFER is two readers reaching the same answer by different routes: fixture proposes TRUE and leans on the gate, model proposes UNVERIFIED directly). Receipt: compare() with both lines, 1 Aug.
12c. **The interpretation field's empty state is well-formed.** Charter 1.1.0 returns the key on every finding with an empty string, meaning "no reading beyond the text was needed" — distinct from null, which would mean the field stopped being emitted. Plumbing proven by a mocked-population test; **never yet non-empty in a live run**, because the mini-rubric contains no under-specified proposition by design. Receipt: live check 1 Aug.

12-old. **The one miss, fully diagnosed, and the structural finding it produced.** On transcript one the model returned FALSE with full coverage; ground truth is UNVERIFIED with partial coverage. The interpretation field recorded the cause verbatim: it treated *the seller's question* ("is that this year's money or next?") as covering element e3, then read the buyer's refusal to answer as the element failing. **Span verification proved the words existed; it cannot prove the words mean what was claimed.** Every deterministic check behaved correctly and the wrong verdict still passed, because the only check that asks whether spans establish an element is Model Call 2, which is not yet wired. Empirical proof that the four checks are non-substitutable, from the first live attempt. Receipt: compare output, 31 Jul 12:01.
13. **The interpretation field earns itself on first use.** Designed 30 Jul, live 31 Jul; converted an unexplainable verdict disagreement into a one-line diagnosis. Without it the miss would be visible but not attributable. Receipt: same run.
13a. **model_missed measurable for the first time.** Reachable only once a model actually reads a transcript; both passes recorded 0. Receipt: same run.
13b. **A corrected end state does not undo a wrong intermediate.** The Gate 1 deal's final answer reads TRUE because transcript two overwrote the FALSE. On a live deal that intermediate FALSE would have been a committed claim on a gating proposition, a red flag, and a possible disqualification input. The blind reader's value is stated here as a consequence, not a principle.

## NEEDS MEASUREMENT, test named

14. **Token cost per answered question vs re-read architectures.** By construction: keyed reads cost zero model tokens. Baseline 31 Jul, uncached: 3,137 in per extraction pass. **With the rubric prefix cached (1 Aug): 175 paid in + 3,187 read from cache — a ~95% paid-input reduction. The blind reader adds 1,283 in / 591 out (~9s), so the second verification pass costs less than the caching saves.** Quote the check's cost beside its benefit, always. **Caveat that belongs next to the number: the cache TTL is ~5 minutes, so the discount holds only for passes clustered inside the window** (observed: cacheRead=0, cacheWrite=3187 after TTL expiry). Consequence for the three doors: the nightly sweep clusters passes and earns the discount; the trickle door mostly does not. Batch scheduling is an economic decision, measured, not assumed.
15. **Latency per pass at scale** (the 25-transcript day). First point: 13.7s single pass.
16. **Abstention rate by charter version.** Detector built (candidate reasons by charter). Baseline accumulates from round two.
17. **Small-talk boundary accuracy.** Labelling exercise, ruled; boundary marked not cut, so the test stays runnable forever.
18. **Third-party signals as attention pointers.** Gong topics as hints that narrow the read, never establish. Design compatible (derived classifications read, never trusted); test later.
19. **Blind reader catch rate.** First data point recorded: one specimen predicted, one caught (rows 12a-12b). Accumulates from discovery-driven passes. **The telemetry split is now three-valued** — nobody_said (evidence does not bear on it), model_declined (bears on it, would not commit: the true abstention), not_returned (the model never reported the proposition: a charter-quality signal, never counted as abstention) — ruled 1 Aug after model_missed was found conflating them; Outcome gains Not_Returned by the same ruling. New enum values proven in tests, **not yet observed live**; first live writes come with the next real pass. Both readers are now named on every candidate row (extraction charter + blind charter, each with version), so any outcome is attributable to the rules that produced it.
19a. **Charter FALSE-bar calibration.** The miss in row 12 has two independent causes: seller speech used as an establishing span (the blind reader's job) and an over-eager FALSE on an open answer (the charter's own bar). Tighten the charter instruction so absence-never-establishes-FALSE is explicit; measure whether the FALSE disappears before the blind reader is even consulted. **The speaker-rank rule stays out of the charter deliberately, so the gate can be observed firing.**

## RESOLVED — was unverified, now checked

20. **~~Data 360 has a native Gong connector~~ — WRONG, corrected 31 Jul.** Said to Toby on the 31 Jul call from memory; verified against Salesforce documentation the same day and it is not true. **There is no Gong connector and no Teams-transcript connector.** Documented native sources: Salesforce CRM and the marketing/commerce clouds, cloud storage (Amazon S3, Microsoft Azure, Google Cloud Storage, SFTP), warehouse federation and sharing (Snowflake, BigQuery, Databricks, SageMaker), MuleSoft Anypoint, and the Web/Mobile SDKs. **The real answer is three paths, none dependent on a vendor connector existing:** (a) Gong's own Salesforce integration writes calls and activity into core, which the CRM connector ingests — free, and the same is true of ECI; (b) enterprises commonly land transcripts in S3/Azure/GCS/SFTP for retention, all natively connected — configuration, not code; (c) the **Ingestion API**, a documented REST interface with streaming (incremental) and bulk (CSV) patterns, OAuth via connected app, built explicitly for sources without a native connector. Because we define the payload, every source is the same integration shape: **one payload contract to maintain, not one integration per vendor.** Corrected to Toby in writing 31 Jul. Receipt: Salesforce Data Cloud connector catalog + Ingestion API guide, retrieved 31 Jul 2026.

26. **Cost to reach the demonstrated state, receipted.** Seed document to model-verified, replay-proven pipeline: ~48 hours, one architect plus AI tooling, zero new infrastructure, zero new vendors, token spend under a dollar, sandbox only. This is not a claim about finished-product cost; it is the measured shape of the cost curve, and it exists because everything expensive about an external build (the second system) already existed inside the platform. Receipt: repo history 30 Jul–1 Aug, journal, token logs.

27. **Language guardrail: Trust Layer is target, not current.** Model calls today go directly to the Anthropic API via Named Credential; the Einstein Trust Layer gateway is the packaged endgame and is drawn on the target-architecture diagram as such. Safe sentence: "runs inside the customer's org, with the Trust Layer as the packaged model path." Unsafe: "every model call already runs through the Trust Layer." Recorded so the two never blur in anything Toby-facing.

## UNVERIFIED — do not repeat externally
21. **Foundations credits make the PoC free for every customer.** Verify the entitlement story.
22. **All Altify customers on Salesforce Enterprise with Trust Layer available.** Verify.

## Structural arguments (argued in the documents, evidenced as measurements land)

23. **Determinism requires binary propositions and closed vocabularies**; a graph store's history is stored inference, re-derived per ask, non-deterministic by construction. Ours is receipts under keys: same question, one lookup, same answer.
24. **Enterprise trust posture.** Everything inside the customer's existing Salesforce boundary; no second system, no new DPA, no data egress; every value answers "why does this say this" in one hop.
25. **Cost shape.** Always-monitoring spends with data volume forever; AAO spends on new evidence a bounded number of times, then keyed reads.

*Append with receipts. Corrections mark the old row wrong rather than deleting it.*
